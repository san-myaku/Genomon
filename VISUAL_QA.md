# VISUAL_QA.md — ビジュアル評価

## 未修正の重大な欠陥: 模様が顔を覆っても自動検査が検出しない

**発見の経緯**: ビジュアル批評担当が「Critical finding.」と書いた直後に上限で中断し、内容が記録に残らなかった。リードが同じ画像群を精査して該当個体を特定し、根本原因まで追跡した。

**現象**: `PHS4-DYQB`（slime/wavy lagoon、質感 frost、模様 **cow**）は、牛柄の暗い塊が目の間から口の上に居座り、**口が完全に隠れている**。顔は「閉じ目＋灰色の口ひげ」に見える。幼体・成体の両方で同じなので、段階ではなく**模様配置**の問題。

**にもかかわらず Visual Lab は「検査 OK（inspectModel で問題なし）」と表示する。**

**根本原因**（`src/render/inspect.ts` の顔被覆チェック）:

```ts
const DECOR_IDS = new Set(['antennae', 'horns', 'plant', 'crystal', 'collar']);
const FRONT_Z = 58;   // Z.PATTERN は 48
...
if (!p.bbox || !DECOR_IDS.has(p.id) || p.z < FRONT_Z) continue;
```

`pattern` は **`DECOR_IDS` に含まれておらず**、さらに **z（48）が `FRONT_Z`（58）未満**なので、二重に除外されている。模様が顔を全面的に覆っても検査は素通りする。

**これは再発である**。以前「`boxProbes` が bbox の角と辺の中点しか見ないため尾の接触点を取りこぼす」という誤検出を直したが、根っこは同じ——**検査の対象範囲が、画面に実際に現れるものと一致していない**。

**修正が単純でない理由**: 模様は本体シルエットで `clipPath` されるため、その bbox は体全体になる。bbox ベースで判定すると**模様を持つ全個体が引っかかる**。したがって「模様が実際にどこへ図形を置いたか」を `pattern.ts` 側から検査へ渡す必要がある（`faceCoreOf()` の回避判定で既に座標を計算しているので、それを外へ出せばよい）。

**あわせて確認すべき**: `cow` の顔回避は「横へ逃がす」実装だが、`slime/wavy` のように faceBox が横に広い個体では逃がし先が顔の中に残る可能性がある。検査の穴を塞ぐだけでなく、回避そのものが効いているかを実測すること。

---

## 現在の判定: **独立した批評担当による再評価が未完了**

下記 16 項目の採点（平均 5.13）は **P0 の描画バグ8件を直す前・追加7要素の実装前**のもので、**現状より大幅に低く出ている。**

独立した批評担当による再評価を 2 回試みたが、いずれもセッション上限で中断した。**したがって「合格」とは言えない。**

### リード自身による所見（独立した批評ではないことを明記する）

`docs/screenshots/art4/d5-adult-25.png` を見ての評価。批評担当の代替にはならないが、記録として残す。

| 項目 | 前回 | リード所見 | 根拠 |
|---|---|---|---|
| パーツの一体感 | 3 | **大幅改善** | 脚の透けが完全に消失。装飾の付け根も自然 |
| 顔 | 4 | **大幅改善** | `7UDK-894T`（閉じ目）`EVHY-ZR2E`（重いまぶた）`J6QL-RKSK`（細めた目）`DBQ2-3FH6`（単眼）が一目で別個体 |
| 表情 | 6 | 改善 | 閉じ目・眠たげ・困り顔・満面の笑みが揃う |
| 模様 | 4 | 改善 | 14 種になり、25 体中の大半に模様が乗る |
| 配色 | 5 | 改善 | 暖色13対寒色11。潜性の `mineral`（`EVHY-ZR2E`）も出現 |
| シルエットの多様性 | 5 | 改善 | `UFDD-FJQL`（くびれ）`A8BC-G7N4`（尖った雫）`9GLJ-NT7N`（ゆらぎ）が別物 |
| 美しさ | 4 | 改善 | 描画不良は解消。ただし弱い個体は残る |
| 質感 | 5 | **やや改善に留まる** | veil / jelly / frost は区別できるが、16 項目中いちばん弱い軸のまま |

**リードが見て最も弱いと判断した個体**: `A8BC-G7N4`（yurei/pointed coral）。中央のまだらがシミに見え、目も小さく顔が弱い。

### 最後の2件（リードが実物で確認）

スクリーンショットは `docs/screenshots/art5/`。

**暖色 `dartNet` のコントラスト → 解消**（`03-dartnet-warm4.png`）
同一 seed 9 体を ember / coral / bloom / dusk の 4 配色で並べた比較。網が茶ではなく**黒**になり、鮮やかな地とのコントラストが出た。「ひび割れた土」「メロンの皮」の印象は消え、**明るい鮮やかな地 × はっきりした暗い網目**というヤドクガエル本来の構成になっている。36 体すべてで顔も読める（前回の顔回避も保たれている）。4 配色とも検査 0/9。

**質感 8 種 → 改善（ただし完全ではない）**（`08-texture8-slime.png`）
同一 seed・同一配色・模様なしで 8 種を並べた比較を作らせた。これが正しい検査方法だった。

| 明確に区別できる | 見分けにくい |
|---|---|
| `pearl`（虹色の干渉色）／`mossy`（緑の粒）／`mineral`（直線の面割り）／`glassy`（強い屈折ハイライト） | `matte` / `jelly` / `frost` / `veil` |

後者 4 種も「艶と白さの度合い」で順序は付いており、`matte`（鏡面反射を一切置かない）→ `jelly`（内部に沈む光＋底の透過光）→ `frost`（白い霞で内部が見えない）→ `veil`（ハイライトなし・均一に薄い）と設計上の作り分けはある。**最も近いのは `frost` と `veil` の組。**

「全個体が同一の艶ゼリー」という当初の指摘は解消したが、**16 軸中いちばん弱い軸であることは変わらない。**

**残る未解決**: `frost` と `veil` の見分けにくさ。



厳格なビジュアル批評担当（実装担当とは別のエージェント）による評価。
**16項目中15項目が7点未満、平均 5.13/10。** 合格基準（全項目7点以上・平均8点以上）を満たしていない。

評価は実際のスクリーンショット（`docs/screenshots/` の51枚）を見た上で行われた。

## 採点

| 項目 | 点 |
|---|---|
| 第一印象 | 6 |
| かわいさ | 7 |
| 美しさ | 4 |
| 独自性 | 6 |
| シルエットの多様性 | 5 |
| 配色 | 5 |
| 模様 | 4 |
| 質感 | 5 |
| **パーツの一体感** | **3** |
| 顔 | 4 |
| 表情 | 6 |
| 成長差分 | 5 |
| 親子の類似性 | 5 |
| UIとの調和 | 6 |
| また生成したくなる魅力 | 5 |
| 個体へ愛着を持てる可能性 | 6 |

**平均 5.13 / 10**

## 最も重要な指摘: 自動検査が機能していなかった

コンタクトシートのヘッダは「**問題個体 0 / 9**」と表示していたが、その9個の卵には明白な破綻が複数あった。`inspectModel` が「体から離れたパーツ」「輪郭外へのはみ出し」を検出できていなかった。

**検査が素通りする限り、同種のバグは何度でも再発する。** 実装より先にここを直す必要があった。

## 現在の実測（この評価より後に進んだぶん）

成体300体（seed=AUDIT）:

```
素体: maru=111 yurei=95 slime=94
配色: bloom=55 coral=48 dusk=40 lagoon=38 ember=36 frost=34 meadow=32 moss=12 pearl=5
      → 暖色 59.7% / 寒色 38.7% / 潜性 1.7%（評価時は寒色67%・暖色29%）
自動検査: 0 / 300
```

`pearl` と `mineral` は Visual Lab の配色強制で初めて 36 体ずつ確認できた。**`mineral`（こうせき）は10系統中もっとも見応えがある**が、`pearl`（しんじゅ）には2つの問題があったのでリードが `color.ts` を修正した。

1. 明度88・彩度14 の個体は紙（`#f8efdf`、L≈93）とほぼ同値で、輪郭インクが無ければ消えていた → 明度上限 82・彩度下限 18 へ
2. `accentShift` の 130 が色相 26〜62 から 156〜192（緑〜青緑）へ飛び、模様や装飾が強い個体が「こけ」と区別できなくなっていた → 真珠の虹色として自然な 210（菫寄り）へ

## 追加要望の7項目（ユーザー指定）— 実装済み・リードが実物で確認

無彩色 / 2色グラデーション / 半透明 / 発光 / 牛柄 / ヤドクガエル柄数種 / 星屑。
遺伝子座と配色の定義はリードが、描画はアート担当が実装した。

| 項目 | 遺伝子 | リードの評価 |
|---|---|---|
| 無彩色 | 配色 `ash` | **良好。** 灰の体に「一点だけ色」が成立し、塗り忘れには見えない。灰の6段グラデで光を暖色・陰を寒色へ振る処理が効いている |
| 2色グラデ | `bicolor` = ふたいろ/ななめぼかし/すそぞめ | **良好〜一部弱い。** ふたいろ・すそぞめは明確。**ななめぼかしは9体中3〜4体で読み取りにくい**（淡い配色同士だと差が出ない）。`ash` との組み合わせ（灰→色）が最も美しい |
| 半透明 | 質感 `veil` | **良好。** ハイライトを一切置かないことで jelly / glassy と区別できる。膜越しに耳・足・尾の付け根が透ける処理で「薄い色」ではなく「透けている」と読める |
| 発光 | `lumin` = うちあかり/ふちひかり/つぶあかり | **良好。** 明背景では周囲を落とすヴィネット、暗背景では芯の明度で光を描き分けている。**ただし明背景の `inner` は個体によってはかなり控えめ**（白い紙の上の発光としては妥当な範囲） |
| 牛柄 | 模様 `cow` / `bellyCow` | **良好。** lite 描画の小サイズ36体で**全個体が識別可能**。塊は大きく不規則・非対称・輪郭で切れており円になっていない。無彩色との組み合わせがホルスタインらしい |
| ヤドクガエル柄 | `dartBand`/`dartNet`/`dartDrop`/`dartPebble` | **4種とも別実装で良好、ただし暖色系で弱い**（下記） |
| 星屑 | 模様 `stardust` | **今回の最高到達点。** `lumin: mote` との組み合わせで「夜空を体に閉じ込めた」絵になる。粒の大小・密度の偏り・4方向の光条がすべて効いている |

## 追加要望の後に出た指摘とその後（リードが実物で再確認）

スクリーンショットは `docs/screenshots/lead/`。

| 指摘 | 現状 |
|---|---|
| **まだら（dapple）が「発疹・カビ」に見える** | **解消。** 斑が同系色の明度差のみになり、色相を跨がなくなった。`H4LG-5V7J`（ember）・`89G8-VGPJ`（ember）は自然な肌の質感として読める。以前の「生肉」「カビたパン」の印象は消えた（`dapple-warm.png`） |
| **slime のシルエット分散がほぼゼロ**（16体中14体が同一ドーム） | **解消。** `Y3MH-7G2B`（横広の低いドーム）／`C74D-2B2C`（縦長）／`6P2S-MH54`（波打つ裾）／`9X42-BR83`（尖った頂点）が明確に別物として読める。tall / wide / wavy / pointed が slime でも効くようになった（`slime16.png`） |
| **dartNet が暖色系で低コントラスト** | **部分的に改善、未完了。** `Z2G5-RRL7` は網が濃くなりコントラストが上がった。ただし **`C2T7-U9L6` は網が顔を横切って目と口を覆っており**、まだ「ひび割れた土」寄り。寒色系（`WKTN-Q7YK` 紫・`X8PW-GXXZ` 水色・`J4GS-4PA6` 桃）は狙いどおり美しい（`dartnet-warm.png`） |
| 口の大きさ・顔レイアウトの固定 | **未確認。** 担当がセッション上限で中断した時点で `face.ts` の口の正規化に着手していたが、完了と検証には至っていない |

### 顔まわりの決着（リードが実物で確認）

スクリーンショットは `docs/screenshots/art4/`。

| 項目 | 結果 |
|---|---|
| dartNet の網が顔を横切る | **解消。** 網が目と口の周りで細く薄くなり、濃い線は頭頂だけに残る |
| **dartDrop（したたり）が顔全体を覆う** | **解消。** リードの指示範囲外だったが担当が自ら発見。滴は「面」なので細くできないため、①体側へ寄せる ②額で止める ③あごの下から流す の3段で逃がす。**一律に額で止めると全個体が前髪のようになる**ため seed で振り分けている。9体すべてで目と口が読めることを確認 |
| dartPebble の粒が目に食い込む／口を覆う | **解消。** 牛柄と同じく横へ逃がす（下だと全個体で口ひげになる）。寄せ先にばらつきを入れ「額縁」化を回避 |
| dartBand の帯が口にかかる | **解消。** 従来は faceBox の高さだけを見て**帯自身の太さを見ていなかった**。区間の重なりで判定するよう変更 |
| **口が目に対して小さい** | **解消。** 400体の実描画計測で口幅/目幅の中央値 **1.61**、**面として開いた口 24.1%**（以前ゼロ）。96px の lite 100体でも引きで消える個体なし |
| **顔レイアウトが全個体固定** | **解消。** 300体の実測で faceW/bodyW **0.664〜0.945（sd 0.063）**、faceH/bodyH 0.420〜0.560、faceCy 相対位置 **0.314〜0.526（sd 0.049）**。eyeSize / eyeSpacing も描画に届いている（目の幅 p10 23.1 → p90 60.4） |

**自動検査**: 成体300体・幼体200体・卵200体＝**700体すべて0件**。ヤドクガエル4種の強制生成シートもすべて0件で、件数は増えていない。

**副作用として記録**: dartBand は帯を置ける帯域が上下2つしかない（顔が体の中央を占めるため）ので、修正後は「頭頂の細い帯＋裾の帯」の組み合わせが増え、配置のバリエーションがやや減った。担当は破綻より地味を選ぶ判断をしている。

**残る未解決**: 暖色（ember）の `dartNet` は網が黒ではなく茶に見え、「ひび割れた土」寄りの印象が残る。**顔の可読性とは別のコントラストの問題**。

---

**（以下は追加要望の実装直後の記録）**

**dartNet の暖色系での問題（リードが実物で確認）**

`Z2G5-RRL7` と `C2T7-U9L6`（ともに ember 配色）は、茶の網が黄土色の地に乗って低コントラストになり、ヤドクガエルというより**「ひび割れた土」「メロンの皮」**に見える。指定した明度差の保証（`pattern L ≥ body L + 38`）が暖色系で効いていない。寒色・高彩度の配色（`WKTN-Q7YK` 紫、`X8PW-GXXZ` 水色、`J4GS-4PA6` 桃）では狙いどおり美しい。

**まだ残るもの**: `dapple`（まだら）が暖色個体で「発疹・カビ」に見える問題は、以前の批評での指摘から未解決のまま（`4D5C-3L9E` で確認）。

新機能の実装後も自動検査は増えていない（成体300体・幼体200体・卵200体でいずれも 0 件、コンタクトシートの各強制生成でも 0/9〜0/36）。

## 対応済み

| 指摘 | 対応 |
|---|---|
| 自動検査が破綻を検出しない | 検査を強化。100体で3件（not-touching-body:1, face-outside-body:4）を検出するようになった |
| 影のbboxが実寸の2.4倍・4.8倍で誤検出（25体中13件） | ぼかし半径（3σ）から実寸で算出するよう修正。100体で誤検出が消えた |
| 目の形が実質2種類（16体中13体が同じ大きな丸目） | 白目の形状を leaf / wide / sleepy で別パスに分離。**点目（白目を持たないインク一色の目）を追加**し「まるめ／たまご × つぶら」で発現するよう結線 |
| 『まるめ』が支配的（dominance 5・weight 26） | 遺伝子カタログで dominance 4・weight 16 へ。他の目の形の重みを引き上げ |
| 模様の約7割が無地 | 『むじ』を weight 20→12・dominance 5→4 へ |
| 配色が寒色67%・暖色29%に偏る | 暖色4種の重みと顕性度を引き上げ。**100体で寒色44%・暖色56%に反転**（実測: meadow16 ember15 dusk15 coral13 bloom13 lagoon12 frost11 moss5） |
| ダークモードで接地影が消える | ライト/ダークで別の影（暗背景では「光の溜まり」）を描き分け |

## 前版から改善済みと確認された点（批評担当も合格と認定）

- **体の縞の湾曲** — バーコード状の等間隔直線から、体の丸みに沿って湾曲する帯へ。`UFDD-FJQL` `A8BC-G7N4` `5YXV-VB9B`
- **本体グラデの彩度** — 白に飛んでいた coral / ember / dusk が本来の色で読めるように。`WUYZ-S7QE` `EVHY-ZR2E`
- **足の面としての描画** — 針金状の stroke から、付け根を体内部から始める面描画へ。`223F-VNQ4` `J2ER-LEF4`

## 未修正の指摘（正直な申告）

セッションの制約により、以下は**未対応**のまま残っている。

### P0（描画バグ）— **8件すべて解消。リードが実物を見て確認済み**

担当エージェントは2度セッション上限で中断したが、中断前に P0 の修正は入っていた。リードが `sheet.html` で実際に描画を見て、8件すべての解消を確認した。

| # | 指摘 | 確認結果 |
|---|---|---|
| 1 | 脚の体内部分が透けて台形に見える | **解消**。体マスク（`registerBodyMaskOut`）で体内部分を「描かない」方式に変更し、透明度に依存しなくなった。`3BRX-HAEP` `223F-VNQ4` `A83M-P8UW` で確認 |
| 2 | 触角・角・芽の根元の透けと同一形の楕円 | **解消**。同じマスク方式 |
| 3 | まぶた／眉の線が輪郭外へ突き抜ける | **解消**。目尻の外側に残る体内の余白を測って長さの上限にする処理が入った |
| 4 | 卵: 巨大な草が卵の手前を横断 | **解消**。`NGPT-US8Q` は根元の小さな草だけになった |
| 5 | 卵: 放射状（風車）アーティファクト | **解消**。`FDLZ-T9CU`（最悪だった個体）が均質な淡青の卵に |
| 6 | 卵: 成体の耳/角が幾何ブロックとして破綻 | **解消**。`3BRX-HAEP` から突き出ていた四角と菱形が消えた。検査にもルール8（卵に成体装飾が出ていないか）を追加 |
| 7 | 卵: 中央の灰色円がシミ・カビに見える | **解消**。`B4KQ-YHUV` は温かみのある琥珀色の卵になった |
| 8 | 卵: 縞が直線バーのまま | **解消**。`7UDK-894T` の縞が卵の丸みに沿って湾曲するようになった |

あわせて卵全体の彩度が上がり、「卵と成体が別の絵に見える」問題（指摘 6-3）も解消している。

検査には「背面パーツが体マスクを通っているか」（`unmasked-back-part`）が追加され、この種のバグが構造的に再発しないようになった。

### P1（顔）
9. 目の形は**部分的に改善**。白目形状を leaf / wide / sleepy で別パスに分離し、**点目（白目を持たないインク一色の目）**を追加して「まるめ／たまご × つぶら」で発現するようにした。8種への完全な拡張は未完了
10. **顔レイアウトが全個体固定**。未対応
11. **口が目に対して小さすぎ引きで消える**。未対応

### P2（一体感・質感）
12. **装飾がフラット塗りで本体と別素材に見える**（本体はグラデ、装飾は単色ベタ）。色相制限も未実装で桃×緑の補色衝突が起きる
13. **浮遊する腕**（体と接していない）
14. **体側のコブが全個体同形・同位置**で「イボ」に見える
15. **ハイライトが全個体で同一位置・同一形**

### P3
16. **まだら模様がカビ・皮膚病に見える**（暖色ベース×緑/黄の斑）
17. 模様のコントラストが低い
18. **slime のシルエット分散がほぼゼロ**（16体中14体が同一ドーム）。slime は人口の約1/3
19. **幼体→成体で差分が出ない個体がある**（25体中7体）
20. 幽霊素体が接地影から浮いている

### リード判断で変更しないもの
- `pearl`（しんじゅ）/ `mineral`（こうせき）配色の低出現率（約2.5%）は**意図した設計**。保因が個体詳細と交配画面に表示され、狙って交配する動機になる（DESIGN_DECISIONS D-015）
- 装飾が何も無い個体が約1/3あるのも意図（指示書 §11「すべてが派手な個体にならない」）。ただし批評担当の指摘どおり、素体と顔の分散が上がって初めて「地味」が「未完成」に見えなくなる

### リード判断が保留のもの
- `WK3J-DRC7` 系（ベージュ横広丸体＋頭頂の丸耳＋頬紅）が既存マスコットに近い件への対応方針

### 保留から解決したもの
- **OS 絵文字の置き換え（済）** — ステージ内の装飾（🏮・📚）、サイドバー、ゲージ、世話ボタン、ショップ、展示会の観客まで、`src/ui/icons.ts` の手描き線画 56 個に全面差し替えた。単線・角丸・`currentColor` で、生きものの輪郭と同じ画風に揃えてある。詳細と、その過程で出した 3 つの表示バグは DESIGN_DECISIONS **D-026**

## 顔の作り直し（製品オーナー方針・2 回）

### 1 回目：目と口をカートゥーン的に単純化
批評が求めた「口を大きく」を**反転**させた回。批評の症状（引きで口が消える）は本物だったが、処方（大きくする）が誤りだった。可読性は幅ではなく線で取り直した。

| 指標 | 前 | 後 |
|---|---|---|
| 口幅 中央 / max | 63.4 / 90.7 | **38.3 / 51.4** |
| 口幅 ÷ 白目幅 中央 | 1.99 | **1.21** |
| 面として開いた口 | 25.0% | **14.5%** |
| 目 1 つの描画要素数 中央 | 11 | **6** |
| 目と口の間隔が負 | 6 体 | **2 体** |

廃止したもの: 下唇の帯（＝人間の唇に見えていた）、開口笑いの歯の面、口角の跳ね上げ、虹彩の陰・上明かり・縁の 3 層、小ハイライト。
新設したもの: `irisAsp`（虹彩の縦横比）。縦長の白目に真円の虹彩を入れると上下に白が残り**白目を剥いた人間の目**に見えていた（`WUYZ-S7QE` `3ZC7-B8YR`）。

リード判断で `pout`（への字）を潜性から外した。方針が基本形と定めているのに実測 2% しか出ていなかったため。実測 4000 体で にこり 42.6% / ちいさい 28.8% / なみなみ 9.8% / **への字 9.2%** / ぽかん 8.9% / なし 0.7%。詳細は DESIGN_DECISIONS **D-027**

記録: `docs/screenshots/face/{36-large,100-small,16-juv,25-dark}-{before,after}.png`

### 2 回目：白目の面積を減らす
方針「通常個体では白目の面積をかなり減らす／白目が大きい目は人間っぽさ・不気味さが出る／基本は黒目主体の丸目・点目・小さく単純な虹彩／白目がはっきり見える目は少数派／視線が強すぎない、やわらかく単純な目」。

白目の見え割合 `pVis` を `SVGGeometryElement.isPointInFill()` で実測（成体 200 体）。

| | 前 | 後 |
|---|---|---|
| pVis 中央値 | 0.428 | **0.206** |
| pVis ≥ 0.40 の個体 | 60.5% | **11.0%** |
| pVis ≥ 0.50 の個体 | 24.0% | **8.0%** |
| 白目を持たない個体 | 16.0% | **26.5%** |
| 点目 | 10% | **20.5%** |

**途中で方針を取り違えかけた。** 描画側で減らし切ったら pVis ≥ 0.40 が 1.0% になり、「白目がはっきり見える目」の帯そのものが消えた。方針は「無くす」ではなく「少数派にする」。リードが `eyeShape` の『ぱっちり』を weight 15→8（発現 24%→10.8%）に下げ、その少数派になった形にだけ白目を戻した（戻し幅は提案の 0.62 ではなく、目視で 0.68 を採用）。`slit` の瞳がヤギ・ヘビ寄りに見えた件も併せて対処。詳細は DESIGN_DECISIONS **D-028**

記録: `docs/screenshots/face/{36-large,100-small,25-dark,16-juv}-{before2,after2,after3}.png`

## 良い点（維持すべきもの）

- **状態が表情に出る仕組み** — 半目＋への字口で一目で不調と分かる。強い愛着装置。リードも実機で確認
- **閉じ目（^^）の個体** — シート中で最もかわいいと評価された。目の形を変えるだけで印象が激変する実例
- **半透明の虫翅** — 質感表現で唯一成功している例。幼体→成体で翅が生えるのも成長差分として理想的
- **単眼個体** — 識別性が高い
- **独自性の芽** — 吸盤列（`8EVZ-LCFM`）、極端な横広（`2CWY-XPJX`）、等高線模様（`QE5K-CYG2`）。この方向を増やすと既存IP回避にも効く
- **UIの構図** — 生きものを大きく中央に据え周囲に余白をとった育成室の構成

## 記録

スクリーンショットは `docs/screenshots/` に51枚。
- `sheet-adult-100.png` / `sheet-adult-25.png` — 100体・25体コンタクトシート
- `sheet-egg-9.png` / `sheet-egg-25.png` — 卵
- `sheet-juvenile-25.png` — 幼体
- `sheet-base-{maru,yurei,slime}.png` — 素体別
- `sheet-dark-25.png` — 暗背景
- `sheet-debug-9.png` — 描画境界・アンカー表示
- `pc-*.png` / `mobile-*.png` — 実ゲーム画面（PC 1280×860 / スマホ 393×851）各9画面
