# DESIGN_DECISIONS.md — 『ゲノモン』設計判断の記録

各判断は「何を選んだか」「なぜか」「何を捨てたか」を残す。

---

## D-001. 既存資産の扱い — artsample を美術方針の基準にする

**調査した既存資産**

| ファイル | 更新日 | 技術 | 内容 |
|---|---|---|---|
| `artsample/index.html` | 2026-05-30 | **SVG** | 「ゲノモン — アート試作」。淡い紙背景 `#fdf4e6`、太いインク輪郭 `#4b3b34`、目1〜3個、ねこ耳/葉/角/ひれ、feTurbulence による有機的な模様 |
| `genomen_v2.html` | 2026-04-20 | Canvas 2D | 暗色 UI。`ファイア`/`ウォーター`/`エレキ` などのタイプ制、レアリティ、アーキタイプ |
| `poison_frog_..._v8.html` | 2026-04-09 | Canvas 2D | 同系統のより大規模な生成器 v4.7.2 |

**判断**

1. **美術方針は `artsample` を採用する。** 指示書 §1 の優先順位「日付や版番号がある場合は、より新しい資料を優先する」に従い、3つの中で最新（2026-05-30）である。加えて、`artsample` は唯一「ゲノモン」という名前で作られており、その淡い紙・太いインク輪郭・やわらかい配色は、指示書 §16 が求める世界観（標本帳／植物研究室／温室／淡い紙／柔らかい光）と一致する。他2つの暗色 UI はこの世界観と正面から矛盾する。
2. **`genomen_v2.html` / `poison_frog` のタイプ制は採用しない。** `ファイア`／`ウォーター`／`エレキ`／`フェアリー`／`ひこう` といったタイプ名と、common/rare/epic のレアリティ段階は、既存 IP を強く連想させる。指示書 §2「既存のゲーム、キャラクター、IP を模倣してはならない」に反するため、この構造ごと置換する。
   - 代替: タイプ制の代わりに**配色ファミリー**（くさはら／こけ／しも／みずうみ／たそがれ／はなびら／さんご／おきび／しんじゅ／こうせき）を遺伝子座として持たせる。これは属性ではなく色の系統であり、戦闘のない本作の設計に合う。
   - レアリティは段階ラベルではなく、**加点理由が言語化される希少度スコア**にする（§11「珍しさは装飾数ではない」）。
3. **再利用するもの**: 3ファイルに共通する「seed 付き PRNG → 遺伝子 → 描画」という構造そのものは正しいので踏襲する。`artsample` の以下の具体的手法を発展させて使う。
   - `feTurbulence` + `feColorMatrix` + `feFlood` + `feComposite` による有機的な斑模様（ヤドクガエル／斑入りの葉）。ラスタ描画では出せない質感であり、これが SVG を選ぶ主要因の一つ。
   - `clipPath` で本体シルエット内に模様を閉じ込める手法。
   - 白目＋虹彩＋瞳＋ハイライト2点＋上まぶた、という目の構成。

**捨てたもの**: 旧2ファイルの Canvas レンダラ、タイプ制、レアリティ段階、アーキタイプ名。

---

## D-002. 描画技術 — SVG を主、ラスタ化を従とする

**選択肢**: Canvas 2D / SVG / WebGL・Three.js

**判断: SVG（DOM）を主描画とする。**

理由:
- **質感**: `feTurbulence` による有機的な斑・葉脈・苔は、本作の「かわいさ・美しさ」の中核。Canvas で同等品質を出すには自前ノイズ実装が必要で、コストに見合わない。
- **再現性**: SVG は宣言的で、描画順＝DOM 順。`Math.random()` の混入検出も容易。指示書 §13 の再現性要件を守りやすい。
- **拡大縮小**: サムネイルから展示会の大写しまで、同一モデルから劣化なしで描ける。§25「小さなサムネイルでも個体を識別できる」に直結。
- **デバッグ**: Visual Lab で要求される「描画境界・パーツ接合位置・アンカーポイント・描画順の表示」が、DOM 要素の重ね描きだけで実現できる。
- **Three.js は採用しない。** 指示書 §11 が明示的に警告している通り、3D は接合破綻・生成速度・スマホ負荷の全てで不利。本作の絵は 2D のセル画的表現であり、3D にする利点がない。

**トレードオフと対策**: SVG は大量描画に弱い。100体コンタクトシートでは 1 体あたり数十ノード＋フィルタになり、`feTurbulence` を100個並べると実用速度を割る。

対策として `RenderDetail = 'full' | 'lite'` を `RenderModel` に持たせる。
- `full`: フィルタを含む完全描画。育成室・個体詳細・展示会で使用。
- `lite`: `feTurbulence` をベクタの点/線パターンに差し替え、発光ぼかしを簡略化。コンタクトシート・一覧サムネイルで使用。

**重要**: `detail` は**表示品質のみに影響し、遺伝形質・表現型には一切影響しない**。指示書 §25「表示サイズを変えても遺伝形質が変化しない」を型レベルで保証するため、`detail` は `Phenotype` ではなく `RenderModel` の側に置いた。

---

## D-003. 乱数 — 名前付きサブストリーム方式

単一の RNG を全パーツで順に消費する素朴な実装には、致命的な欠陥がある。

```
rng = new Rng(seed)
ears  = rng.pick(...)   // ← ここで1消費
horns = rng.pick(...)   // ← 耳の実装を変えて消費回数がずれると、角以降が全部変わる
```

これは指示書 §13「描画順、画面遷移、UI 操作によって乱数列が変化しないようにすること」に直接反する。パーツを1つ追加しただけで既存の全 seed の見た目が変わってしまい、「問題のある seed 一覧」も無意味になる。

**採用: 名前付きサブストリーム。**

```ts
const rng = new Rng(seed);
const earRng = rng.stream('ears');    // seed + '#ears' から独立に導出
```

`stream(name)` は `seed#name` を種にした独立の RNG を返す。他のパーツが何回消費しても影響しない。パーツの追加・削除・描画順の変更が既存個体の見た目を壊さない。

PRNG は sfc32（高品質・高速）。seed 文字列は xmur3 で 32bit にハッシュしてから 4 語に展開し、初期相関を捨てるため 12 回空回しする。

---

## D-004. 遺伝 — 二倍体（対立遺伝子2つ）

優性・潜性・共優性・保因を成立させるには、1形質1値では足りない。各遺伝子座に対立遺伝子を2つ持たせる二倍体とした。

- **カテゴリ形質**: 対立遺伝子は文字列 ID。`dominance`（優性度）が高い方が発現。同値かつ両方 `coDominant` なら合成表現（例: `leaf` + `flower` → `flowerLeaf`）。
- **数値形質**: 対立遺伝子は 0..1 の実数。表現は平均＋ごく小さいゆらぎ。
- **減数分裂**: 各親から独立に1つずつ選ぶ。これにより兄弟に「共通性はあるが同一ではない」性質が自然に生まれる。

**「なし」を優性にした設計判断**: 耳・角・羽・結晶・首かざり・浮遊物の `none` は最も優性（dominance 6〜7）にしてある。これがないと装飾が全個体に発現し、§11「すべてを同時に付けて派手にすればよいわけではない」「すべてが派手な個体にならない」に反する。逆に珍しい形質ほど潜性（dominance 1〜2）にしてあり、**保因されて数世代後に突然現れる**楽しさを作る。

**突然変異は系統を残す**: 完全ランダムな対立遺伝子への置換ではなく、同一遺伝子座の対立遺伝子配列上で**近い index を優先**して選ぶ。素体（`base`）の変異率はさらに低い（0.6%）。指示書 §14「基本的には親の系統を残すこと」に対応。

---

## D-005. 成長差分は表現型レベルで解決する

幼体と成体の違いを「描画時にサイズを掛ける」だけにすると、§6「単なるサイズの拡大だけにしてはならない」に反する。

`phenotypeOf(genotype, stage)` が stage を受け取り、**表現型そのものを段階ごとに変える**設計にした。

- 幼体: 体は丸く（ratio を 1 に寄せ plump を上げる）、目は相対的に大きく、模様は薄く、発光は弱い。そして **`wings` / `horns` / `crystal` / `collar` / `floaters` を `none` に落とす**。
- 成体: 全形質が発現。

結果として「幼体のときは無かった角や羽が、成体で生えてくる」という驚きが生まれる。同じ遺伝子でも見える形質が段階で変わるため、成長を観察する動機になる。

---

## D-006. 世話を「ゲージ回復作業」にしないための飽き係数

同じボタンを連打すればゲージが戻る設計は §25「世話が単なるゲージ回復作業になっていない」「同じ操作の連打を過剰に要求しない」に反する。

`config.ts` の `BOREDOM` で、**直近に使った世話は効果が逓減し、別の世話をすると回復する**規則を入れた。プレイヤーは自然に「餌 → 水 → なでる → 遊ぶ」と操作を散らすことになり、単調な連打が最適解にならない。

加えて、世話への反応は性格（6軸）によって変わる。同じ「なでる」でも、人なつこい個体は寄ってきて、慎重な個体は少し離れる。

---

## D-007. React を使わない

指示書 §26「React は、画面管理上の明確な利点がある場合のみ使用すること」。

本作の画面数は10程度、状態は単一の `GameState` に集約でき、最も重い描画（ゲノモン本体）は SVG 文字列の生成である。仮想 DOM の差分計算はここでは利点にならず、むしろ SVG の再生成タイミングを制御しにくくなる。

素の TypeScript + DOM + CSS でルーターと画面モジュールを実装する。依存を増やさない。

---

## D-008. 保存 — localStorage + 1世代バックアップ + 連鎖マイグレーション

IndexedDB は非同期 API のぶんだけ複雑になる。本作のセーブは数十 KB 程度（個体は遺伝子から再構成するので画像を保存しない）に収まるため localStorage で足りる。

- 保存キー: 本体とバックアップの2つ。保存前に本体をバックアップへ退避する。
- 本体が壊れていたらバックアップから復旧し、両方壊れていたら `coerceState` で救える範囲を復元する。**壊れたセーブでゲームが起動不能にならないこと**を最優先とした。
- `SAVE_VERSION = 3` とし、v1 → v2 → v3 の連鎖マイグレーションを実装する。v1/v2 は実在しない過去版だが、マイグレーション機構が**実際に動くことをテストで証明する**ために仮想的な旧形式を定義してある（§27「マイグレーション機構を用意すること」を、機構を書いただけで終わらせないため）。

**外見を画像として保存しない**（§13）。セーブに入るのは `Genotype`（seed ＋対立遺伝子）と `LifeState` だけで、外見は毎回そこから再構成する。

---

## D-009. ファイル所有権によるサブエージェントの衝突回避

指示書 §21・§29 が求める通り、複数エージェントが同じファイルを無秩序に編集しないよう、次の規約を敷いた。

1. **型契約の正本 `src/core/types.ts` はリードのみが所有する。** 他エージェントは読み取り専用。
2. 各エージェントに**排他的なディレクトリ／ファイル集合**を割り当てる。担当外のファイルは読めるが書けない。
3. 実装エージェントと評価エージェントを分ける。評価エージェントは書き込み権限を持たず、指摘のみを返す。

| 担当 | 所有ファイル |
|---|---|
| リードアーキテクト | `src/core/**`, `src/genetics/loci.ts`, 各種ドキュメント |
| 遺伝・シミュレーション | `src/genetics/{genotype,phenotype,breeding,similarity,naming,index}.ts` |
| クリーチャーアート | `src/render/**` |
| ゲームプレイ | `src/game/**`（`config.ts` を除く） |
| セーブ・アーキテクチャ | `src/save/**`, `src/game/config.ts` |
| UI・演出 | `src/ui/**`, `src/audio/**` |
| 開発者機能 | `src/dev/**` |
| QA | `tests/**`, `e2e/**` |

---

## D-010. 卵の外見は成体を「わずかに」示唆する

§4「卵の外見がそのまま成体の答えになってはならない」。

卵の描画は遺伝子から作るが、**参照する遺伝子座を意図的に絞り、かつ弱く反映する**:

- 卵の地色 ← `palette` の色相を**鈍らせた**もの（彩度を落とし明度を上げる）。成体の色そのものではない。
- 緑の脈 ← `plant` 遺伝子座に非 `none` の対立遺伝子を**保因していれば**確率的に出る（発現していなくても出る）。
- 結晶質の表面 ← `crystal` を保因していれば出やすい。
- 半透明 ← `translucency` の数値をなだらかに反映。

保因を見るため、**卵の見た目からは「その形質が発現するか」までは分からない**。推測の余地は残るが答えにはならない、という設計。

---

## D-011. 希少度は「飽和しない逓減加点」で計算する

**発見された欠陥**: 最初の実装では希少度スコアが `min(24, n*8)` のような固定上限で加点されていた。リードが独立に検証したところ、**12世代×24体（288体）を交配しても `precious` が1体も出なかった**（最高 score 64 / 閾値 66）。実装担当は「交配で潜性を集めれば出る」と報告していたが、実測は裏切っていた。

**根本原因**（診断して特定した3点）:

1. **固定上限による飽和が主因。** 交配で珍しい形質の同時発現数は実際に増えていた（notable 平均 0.95 → 1.63）のに、3か所を超えると score が1点も増えなかった。**交配で珍しさを集める意味が消えていた。**
2. **notable と潜性の二重計上。** notable な対立遺伝子はカタログ上たいてい dominance ≤ 2 なので、両者はほぼ同一の信号だった（平均 1.63 / 1.66）。見かけ上2項目でも実質1つ。
3. **数値系の加点が死んでいた。** `glow > 0.72` は **500体中0体**、非対称 1%、透明 2%。閾値が厳しすぎて発火せず、実質到達上限が 60 前後だった。

**対処**:
- 固定上限を撤廃し**逓減加点** `decayGain(n, 9, 0.85)` へ。伸びは鈍るが頭打ちにならない。
- 潜性は「notable でない潜性」だけを数えて二重計上を解消。
- 数値系は閾値判定をやめ**連続ランプ＋2乗**（はっきり出ている個体だけに効く）。
- §11 に沿った加点を追加: **配色の調和**（コントラスト比による帯評価）、**珍しい組み合わせ7種**（結晶＋植物 / 透明＋発光 / 目3つ＋非対称 など）、**多面性ボーナス**（3系統目から超線形）。

**結果（実測）**:

| 集団 | rare | precious | score 最大 |
|---|---|---|---|
| 初期卵 500体 | 3.4% | 0.2%（1体） | 66 |
| 交配 3系統 864体 | 10.5% | **1.4%（12体）** | 94 |

飽和が解けた直接確認: 珍しい形質を 0→6 か所に増やすと `score 29 → 35 → 44 → 48 → 58 → 61 → 64`（旧実装は3か所以降フラット）。

**重要な副次条件**: この修正で**装飾器官の個数分布は変わっていない**（`[0個:39, 1個:135, 2個:172, 3個:113, 4個:31, 5個:9]`、平均 1.99）。指示書 §11「珍しい個体ほど、単に装飾数や彩度が増える設計にはしない」を守るため、この分布を回帰テストとして固定した。

**教訓**: 「設計上そうなっているはず」を実測なしに信じない。tier や閾値のような離散的な報酬は、**実際に到達可能かを分布で確認**しなければ死んだ機能になる。

---

## D-012. 幼体では成体の形質を伏せる

遺伝担当からの申し送り: `rarity` と `traits` は成体基準で計算される。これを幼体の詳細画面にそのまま出すと「成体で羽が生える」ことが事前に分かってしまい、D-005 で作った成長の驚きを自ら潰す。

対処として `phenotype.ts` に `visibleTraits(pheno, stage)` を用意し、UI が stage 判定を自前でやらずに済むようにした（自前判定は漏れる）。

- `egg` → 配色と大きさのみ
- `juvenile` → 羽・角・結晶・首かざり・浮遊物の trait を返さない。耳・植物・尾は「いま見えている小さい形」に言い換える
- `adult` → すべて開示

`rarity.reasons` も同じ理由でネタバレを含みうるため、幼体では tier のみ表示し reasons は伏せる。

---

## D-013. クールダウンは個体に保存する（SAVE_VERSION 3 → 4）

**発見された欠陥**: ゲームプレイ担当が正直に申告した未解決問題。展示会の再参加クールダウンと交配クールダウンが、`Creature` 型に置き場が無いためモジュール内の `Map` に保持されていた。**リロードすると消えるので、展示会を連続で回してコインを稼げてしまう。**

**判断**: リードが型契約を変更する。`Creature` に `lastExhibitAt` / `lastBredAt`（ms epoch、0 は未実施）を追加し、`SAVE_VERSION` を 3 → 4 へ。

代替案として「クールダウンを廃止する」も検討したが、展示会の報酬設計（初回最低保証 120 コイン、以降は得点連動）が連続参加を前提にしていないため、クールダウンは残して正しく保存する方を選んだ。

**副次的な利点**: これまで v1/v2 は「マイグレーション機構が動くことを証明するための仮想的な旧形式」でしかなかった。v3 → v4 は**実際に発生したスキーマ変更**であり、機構が本番で機能することの本物の証明になった。

**移行時の値は 0 とした。** `Date.now()` で埋めると、旧セーブのプレイヤーに身に覚えのない待ち時間が発生する。0 なら「クールダウン明け」扱いで、旧セーブでは元々クールダウンが機能していなかったのだから悪化もしない。

回帰テスト（`tests/migrate.test.ts`）: v3 セーブを load → `lastExhibitAt` を刻む → save → 再 load して値が生き残ることを確認する。従来はここで `Map` が消えていた。

---

## D-014. 展示会の5項目は同じ尺度で読めること

「個性」の実測が 16.3/100 と構造的に低く、プレイヤーから見ると常に低い＝**壊れて見える**。展示会は「評価結果を見せる」ことが体験の中心（指示書 §7）なので、5項目が互いに比較可能な尺度で表示されなければならない。

方針: 各項目とも**通常個体で 40〜75、優秀な個体で 85 以上**に収まるようスケールを揃える。閾値をいじるのではなく、低くなる構造的原因（分母・加点条件）を実測で特定してから直す。

---

## D-015. しんじゅ・こうせき配色が滅多に出ないのは仕様

クリーチャーアート担当から「100体シートに `pearl` と `mineral` が 0 体しか出ない」という報告があった。

**調査**: `loci.ts` で両者は dominance 1（他8ファミリーは 3〜4）。発現には両方の対立遺伝子が {pearl, mineral} から来る必要がある。頻度は pearl 8.4% / mineral 7.4% なので、発現率は約 `0.158² ≒ 2.5%`。

**判断: 変更しない。** これは欠陥ではなく、意図した設計である。

理由: ヘテロ保因者は約 27% おり、**`Phenotype.traits` の `carrier` フィールドで「しんじゅ を保因」と UI に表示される**。プレイヤーは保因個体どうしを意図的に交配することで 25% の確率で発現させられる。これは指示書 §14 が求める「目は祖父母由来の潜性形質かもしれない」「潜性形質が実際に発現する」の中核体験そのものであり、ランダム生成で簡単に出てしまってはこの動機が消える。

**ただし2つの条件を満たす必要がある**:
1. 個体詳細画面が保因形質を分かりやすく表示すること（UI 担当の責務）。表示されなければ、プレイヤーには「ただ出ない色」になってしまう。
2. Visual Lab とコンタクトシートで**配色ファミリーを強制指定**できること。そうでないと、この2ファミリーの絵を美術面で検証する手段が無い。

---

## D-016. 自動テストの「最大効率プレイ」は実プレイの代理にならない

リードが**実際にブラウザで最初から手で通しプレイ**したところ、自動テストが全て合格しているにもかかわらず、実プレイでは3体全員が健康0・機嫌0まで落ちた。

**実測（手プレイ約30分）**

```
ヤワヤ 健康3  / 機嫌45 / おなか0 / みず0
キコ   健康0  / 機嫌0  / おなか0 / みず0
ムミモ 健康35 / 機嫌0  / おなか0 / みず0   ← 薬（健康+45）使用直後
```

健康+45 の薬を使っても **約2分で元の水準まで落ちる**（ヤワヤ 40 → 3）。

**根本原因（config.ts の値から計算）**
- 低ステータス1種につき健康 -0.1/秒。4種すべて低いと **-0.4/秒** → 健康 100→0 が **250秒（4分）**
- 健康の自然回復は **+0.06/秒**、しかも**全ステータス60以上が条件**
- **減衰:回復 ≒ 6.7:1 の非対称**。底を打つと手作業では戻せない
- 交配条件は機嫌≥60 かつ 健康≥60。健康0から60まで自然回復させるには **1000秒（16.7分）** 全ステータスを60以上に保つ必要がある

**なぜ自動テストで見つからなかったか（これが本質）**

実装担当のテストは**全個体に4秒ごとに世話をし続ける最大効率プレイ**だった。しかし
1. 実際のプレイヤーはその速度で世話をしない
2. **`SATIATION` 機構がその速度の世話を「非効率」として罰する設計になっている**

つまり、**テストが通る操作パターンと、ゲーム自身が推奨する操作パターンが食い違っていた。** テストは「ゲームが想定するプレイ」ではなく「テストが書きやすいプレイ」を検証していた。

さらに卵3個がほぼ同時に孵化して3体を同時に抱えるため、必要な世話量が最初から3倍になる。

**対処方針**: 「**人間らしい間欠的なプレイ**」（60〜90秒に1回、1体に1アクション）を20分ぶんシミュレートするテストを追加し、その条件下で全個体の健康が20を下回らず、20分以内に交配条件を満たす個体が2体そろうことを実測で保証する。既存の最大効率プレイのテストも並行して維持する。

**教訓**: ゲームバランスの自動テストは、**プレイヤーが実際に取る行動の頻度**を模さなければ意味がない。全項目が緑でも、手で遊べば数分で破綻することがある。指示書 §22 が「実際に最初から操作し、退屈・混乱・作業感・待ち時間を確認せよ」と繰り返し求めているのは、まさにこの穴のためである。

---

## D-017. 実機で確認した seed 再現性

リードが実ブラウザで検証した。育成室でヤワヤの SVG を取得 → **ページを完全リロード** → 再取得して比較。

インスタンス固有の id（`id="..."`, `url(#...)`）を正規化したうえで、**10,774 文字が完全一致**（ハッシュ 656577212）。コイン残高・個体 seed も保持された。

指示書 §25 の「同じ seed から同じ個体が再現される」「保存と再読込の前後で外見が変化しない」「ページを再読み込みした場合」を実機で満たしている。

---

## D-018. API を叩くテストは「遊べること」を保証しない

**発見された欠陥**: 厳格なゲームプレイ批評担当が、実際に遊んで**進行不能（詰み）**を発見した。

成体 3/3・幼体 3/3 の状態で幼体が growth 100 に達すると、ヘッダの目標行が

> 成体の枠が いっぱいです。どれかを 手放すか、先に 育ててください。

と表示する。ところが **「手放す」操作がゲーム内に存在しない**。リードがコードで裏を取ったところ:

```
releaseCreature の呼び出し元:
  src/dev/devMode.ts:580   ← 開発サーバ専用。製品ビルドに含まれない
  src/ui/gameApi.ts:42     ← import しているだけで、どの画面からも呼ばれていない
```

脱出手段はセーブの全消去のみ。**交配を繰り返すこと＝このゲームの目的が、詰みへの最短ルート**になっていた。

**なぜリードの統合テストが見逃したか（ここが本質）**

`tests/integration.test.ts` には「所持枠が上限でも詰まない」というテストがあり、合格していた。しかしそのテストは

```ts
const rel = releaseCreature(s, creaturesByStage(s, 'juvenile')[0]!.id);
expect(rel.ok).toBe(true);
```

と、**プレイヤーが到達できない関数を直接呼んでいた**。ゲームロジックとしては正しく動くので合格する。だが画面にボタンが無いので、遊ぶ人には一切届かない。

D-016 で「自動テストはプレイヤーが実際に取る行動の頻度を模さなければ意味がない」と書いたが、それでも足りなかった。**頻度以前に、経路が違っていた。**

**規約**:
1. 「詰まないこと」を主張するテストは、**UI から到達できる操作だけ**で構成する。game API を直接呼ぶ検証は、ロジックの正しさの証明にはなるが**進行可能性の証明にはならない**。
2. 進行不能に関わる操作（手放す・解放条件・枠の解消）は、必ず `e2e/` 側にも「画面から実行できる」ことを確かめるテストを置く。
3. `src/game/index.ts` が export しているのに `src/ui/screens/` のどこからも呼ばれていない関数は、**未接続の疑い**として扱う。

---

## D-019. 「連打を損にする」設計は、UI が同時に「待て」と伝えなければ成立しない

**発見された欠陥**: ゲームプレイ批評担当の実測（同一ビルド・同一個体）。

| 育て方 | 幼体 → 成体 |
|---|---|
| 連打（2.5秒間隔） | 299 秒 / **114 クリック** |
| がまん（19秒間隔） | 191 秒 / **8 クリック** |

**106 クリック多く押した方が 108 秒も遅い。**

原因は D-006 で入れた飽き係数と、その後 SAVE 担当が追加した `SATIATION.fullMs = 18000`（直前の世話から18秒経たないと効果が最大5%まで落ちる）。設計意図は正しく、実際に連打は損になっている。

**問題は UI の文言だった。**

```
卵  : 「同じ世話ばかりだと 飽きてしまいます。いろいろな 世話を まわしましょう。」
幼体: 「下の細い線は『いまの 効き目』です。短いときは 別の世話を してみましょう。」
```

どちらも「**別のボタンを押せ**」＝より多く押せ、と指示している。正解は「1回押して18秒待つ」なのに、プレイヤーは素直に従うほど遅く疲れる遊び方へ誘導される。§25「世話が単なるゲージ回復作業になっていない」を、メカニクスは満たしていたのに UI が裏切っていた。

**教訓**: メカニクスで「正解の遊び方」を作っただけでは足りない。**その正解が画面から読み取れなければ、プレイヤーにとっては存在しない。** 待つことが正解なら、待ち時間を可視化する（「あと ○ 秒で 全効果」）のが本質的な解決であり、文言の言い換えは対症療法にすぎない。

---

## D-020. `SATIATION.minFactor` は 0 でなければならない

D-019 で「UI が『連打しろ』と誘導していた」文言を直し、「あと ○ 秒で 全効果」のカウントダウンを足した。ところが再評価で、**今度は画面の断定が数学的に偽**であることが判明した。

**効率の式**

効果倍率は `f(t) = minFactor + (1 - minFactor) × min(t / fullMs, 1)`。
プレイヤーにとって意味があるのは「1 秒あたりどれだけ育つか」なので、これを t で割る:

```
rate(t) = f(t) / t = minFactor / t + (1 - minFactor) / fullMs
                     ~~~~~~~~~~~~~
                     t が小さいほど大きい
```

**第 1 項があるかぎり、押す間隔を詰めるほど 1 秒あたり得になる。** 旧値 0.05 では

| 間隔 | 1 秒あたりの効果 |
|---|---|
| 2 秒（連打） | 0.0778 |
| 18 秒（がまん） | 0.0556 |

で連打が 1.40 倍有利だった（批評担当の実プレイ計測では全体クールダウン込みで 1.7 倍）。
画面には「連打しても 早くは なりません。」と**断定形**で書いてあったので、素直に従った人が時間で損をし、疑って試した人は「この表示は信用できない」と学習する状態だった。

**対処**: `minFactor: 0.05 → 0`。すると `rate(t) = 1 / fullMs` で**間隔に依らず一定**になる。

| 計測 | 修正後 |
|---|---|
| 巡回 2 秒間隔 | **0.05556 /秒** |
| 巡回 18 秒間隔 | **0.05556 /秒**（完全に一致） |
| 効果 100 に必要なクリック数 | 連打 **82 回** / 待って押す **10 回** |

速さは変わらず手数だけが 8.2 倍になるので、「待って押すと少ない回数で同じだけ育つ」という説明がそのまま真になった。

**「0 だと無効打に見える」という当初の懸念は解消済み**: 0 でも t=2 秒なら倍率は 2/18 ≒ 11% あり、`TIMING.globalCareCooldownMs = 2000` により実プレイで t=0 は起こらない。加えて効き目バーとカウントダウンが理由を見せている。

**回帰ガード**: `tests/satiation.test.ts` を新設。`minFactor === 0` の直接検査に加え、同一世話・5 種巡回の両方で「間隔を詰めても 1 秒あたりの効率が上がらない」ことと「待つほうがクリック数が少ない」ことを固定した。

**この欠陥は既存のどのテストにも引っかからなかった。** 倍率そのものは仕様どおりに動いており、「1 秒あたりに直すと逆転する」という二階の性質を誰も見ていなかったため。**プレイヤーが最適化する量（時間あたりの進み）で評価しないと、こういう逆転は見えない。**

（テストを書いた当初、巡回の計測だけ 2 秒側が有利に出たが、これは 1 押し目だけ「直前の世話なし」で満足度 100% になり、その得が短い総時間に薄まる**計測側のウォームアップ由来の偏り**だった。助走ぶんを除いて再計測し一致を確認している。）

---

## D-021. 「観察帳」は記録が減ってはいけない

ゲームプレイ批評の再評価で指摘された。本作の副題は「小さな生きものの**観察帳**」で、標本帳は「記録」と説明されている。ところが所持枠は各段階 3 体で、交配で子が生まれるたびに親を手放さざるを得ない（子が成体になる時点で必ず枠が詰まる）。結果として **続けるほど記録が減っていく観察帳** になっていた。

手放しの確認文が「この子は もう 戻ってきません」と丁寧なだけに、その後に何も残らないのが強く効いてしまう。

**対処**: UI 所有の localStorage キー `genomon.ui.released.v1` に「森へ かえした子」の記録（遺伝情報・名前・世代・親名・最高点・手放した日時、上限 40 件）を保存し、標本帳にタブとして表示する。

**セーブ本体（`genomon.save.v1`）のスキーマは変更していない。** 記録は観察の副産物であってゲーム状態ではないため、`GameState` を太らせずに UI 層で持つ判断とした。`GameState.future` は将来のオンライン機能用の予約領域なので流用していない。

**seed だけでは足りなかった**: 交配で生まれた子の姿は親の遺伝子の組み合わせで決まるため、seed から再生成できない。遺伝情報ごと保存している。

副次的な効果として、**片親を手放しても親子比較が出せる**ようになった（D-022）。

---

## D-022. 幼体・卵の画面で `Phenotype.parts` を直接読んではいけない

UI 担当が自力で発見して修正した、D-012 の抜け穴。

`inheritanceHighlights`（親子比較の説明文を作る関数）は `Phenotype.parts` を直接読んでいた。ところが D-012 のとおり **卵・幼体の `Phenotype` は器官の値を成体基準のまま持っている**（`rarity` と `traits` を stage 非依存にするため）。そのため **卵の詳細画面で「成体で生える羽」が先に読めてしまっていた。**

`visibleTraits(pheno, stage)` を用意して「UI は stage 判定を自前でやらなくてよい」ようにしたつもりだったが、**別経路（親子比較）が生の `parts` を読んでいた**。

**対処**: 成体で両親そろっている場合のみ genetics 版の `inheritanceHighlights` を使い、それ以外は `visibleTraits(child, stage)` を通す UI 版に切り替えた。

**規約**: `Phenotype.parts` / `rarity.reasons` を UI から直接読む箇所は、すべて stage による伏せ処理を通すこと。ヘルパを 1 つ用意しただけでは漏れる。

---

## D-023. 単体で測って「直った」と言ってはいけない — 連打の残差

D-020 で `SATIATION.minFactor` を 0 にし、リードは「巡回 2 秒間隔 = 0.05556/秒 / 18 秒間隔 = 0.05556/秒 と**完全一致**」と報告した。**この報告は不十分だった。**

後にリードがゲームロジックを直接叩いて**孵化までを通しで**測ったところ:

| 押す間隔 | 孵化まで | クリック数 |
|---|---|---|
| 2 秒（連打） | **69.8 秒** | 35 |
| 6 秒 | 72.3 秒 | 13 |
| 18 秒（がまん） | **87.8 秒** | 5 |
| 30 秒 | 102.3 秒 | 4 |

**連打はまだ 1.26 倍速い。** 満足度関数だけを取り出すと間隔によらず一定なのに、通しでは一致しない。差の出所は未特定（飽き係数との相互作用か、実時間の受動進行との合成か）。

**教訓**: 一つの関数を単体で測って「直った」と結論してはいけない。プレイヤーが体験するのは**合成された結果**であって、個々の関数ではない。D-016（自動テストの操作頻度が実プレイと違った）と同じ失敗を、今度は「測る対象の粒度」で繰り返した。

**現時点の評価**: 連打は 7 倍のクリック（35 回 vs 5 回）で 26% の短縮なので、人間にとっては明確に損な取引であり、「連打が支配的戦略」という当初の欠陥は解消している。ただし画面の**断定形**「連打しても 早くは なりません。」は厳密には事実と異なる。文言を緩めるか、残差の出所を特定して潰すかの判断が要る。ゲームプレイ批評担当に判定を委ねている。

---

## D-024. 「解消した」という報告は、測った指標の外に穴が残る

ビジュアル批評の再評価で、リードが受け入れていた報告の穴が2つ判明した。

1. **顔レイアウトの分散**: 実装担当は `faceW/bodyW`（sd 0.063）と `faceCy`（sd 0.049）を測って「解消」と報告し、リードはそれを受け入れた。批評担当が **`faceCx` を測ると sd = 0.004**（min 0.489 / max 0.508）で、**水平位置は 100 体すべて中央固定**だった。垂直だけ直っていた。96px サムネイルで「全部同じ顔」に見える主因はこれ。
2. **模様の顔回避**: dart 系 4 種は `faceCoreOf()` で「実際の目と口の実寸」を守るよう直されたが、**牛柄だけが古い粗い判定のまま取り残されていた**。牛柄が出た 8 体すべてで口ひげ・あごひげ・目の上の黒塊になっていた。

**教訓**: 「解消した」の根拠が実測値でも、**測っていない軸には穴が残る**。同種の処理が複数箇所にあるとき（dart 系と牛柄）、片方だけ直して報告されると気づけない。レビュー側は「何を測ったか」だけでなく**「何を測っていないか」**を問う必要がある。

---

## D-025. テーマで色が変わるなら、その色に依存する保証も一緒に変えなければならない

ビジュアル批評が「牛柄の塊とインクのコントラストが 1.02〜1.25:1 で顔の線が物理的に見えない」と指摘し、`cowColor()` に**インクとの 3.0:1 を絶対条件**として入れて解決した（明背景 min 3.01、3.0 未満 0 体）。

**ところがその保証は明背景のインクにしか掛かっていなかった。** 暗背景では `inkOnDark()` が別の色に差し替えるため、リードが測ると **200 体中 143 体が 3.0 を割り、中央値は 1.14:1** だった。**同じ模様が、テーマを切り替えるだけで読めなくなる。**

（これは批評担当が「暗背景での牛柄——インクが切り替わる場所を確認する」と書いた直後に上限で中断したため、リードが引き継いで実測したもの。着眼は批評担当のもの。）

**最初の対処とその限界**: 両方のインクを絶対条件にしたところ、暗背景の不合格は 143 → 20 体まで減ったが、**明背景で 2 体・暗背景で 20 体が両立不能**として残った。たそがれの紫のような中間調の体では、1 色で両方のインクから 3.0:1 離れることが数学的に不可能なため（コード内に計算を明記）。

**採った解**: インクが既に `--gm-ink` でテーマ別に切り替わっているのだから、**塊も同じ仕組みに載せる**。`DrawCtx.themeVars` を追加し、パーツが `[明背景での色, 暗背景での色]` を書き込むと `inkThemeStyle()` が `--gm-cow` として CSS 変数に流す。これで明暗それぞれに最適な色を独立に選べ、妥協が消えた。

| | 修正前 | 両方を条件に | テーマ別の色 |
|---|---|---|---|
| 明背景 min | 3.01（0 体不合格） | 2.64（2 体） | **3.01（0 体）** |
| 暗背景 min | **1.00（143 体）** | 1.77（20 体） | **3.01（0 体）** |

`themeVars` は汎用なので、今後テーマで塗り分けたい色（発光・影・模様）が出たら同じ経路で足せる。

**回帰ガード**: `tests/contrast.test.ts` で明暗**両方**の 3.0:1 を固定した。片方だけ測ると今回の穴がそのまま再発する。

**教訓**: 「A に対して保証する」実装を入れたら、**A が動的に変わる箇所がないか**を必ず確認する。D-024 の「測っていない軸に穴が残る」の具体例であり、同じ失敗を 3 度繰り返している（テストの操作頻度 → 測る対象の粒度 → 保証の対象そのもの）。

---

## D-026. UI のアイコンを絵文字から手描き線画へ替えた／型が通ることは表示が正しいことを意味しない

ビジュアル批評 #9 の未解決項目「UI の絵文字アイコンが、紙とインクの線画である生きものと画風が割れている」への対処。絵文字は **OS 任せのフルカラー光沢絵**で、Windows / iOS / Android で形そのものが変わり、線の太さを本体と揃えられない。`src/ui/icons.ts` に単線・角丸・`currentColor` の 24×24 アイコン 56 個を起こし、全画面で置き換えた。

**移し替えの進め方**: 一斉に `IconName` 型へ変えると型検査が広範囲で赤くなり、途中の状態でアプリが動かなくなる（並行して描画担当が作業していたので、なおさら混乱を招く）。そこで `iconOrText(v)`＝「名前なら線画、そうでなければ文字のまま」を挟み、**常に動く状態のまま 1 ファイルずつ**移した。

**ここで 3 つの表示バグを出した。型検査・186 件のテストはすべて緑のまま通っていた。**

| 出たもの | 原因 |
|---|---|
| 世話ボタンに「flame」「drop」と**英単語が出た** | `${s.icon}` を生で埋めていた箇所を見落とし、`iconOrText` を通していなかった |
| 収容数のピルに `<svg class="ico" viewBox=…` と**ソースがそのまま出た** | `capEl.textContent =` に SVG 文字列を代入していた（要素として入らない） |
| 空状態のアイコンが中央でなく**左端に出た** | `.ico { display:block }` にしたため、親の `text-align:center` が効かなくなった |

いずれも**画面を撮って見るまで分からなかった**。型は「文字列が入っている」ことしか保証せず、それが絵になるか英単語になるかは見ないと判別できない。

**取りこぼしの探し方**: 目視だけでは残りを保証できないので、`${…}` の中にアイコン名を持つ値が `iconOrText` / `icon()` を通さずに入っている箇所を機械的に洗い出した。これで装飾（`d.glyph`）の見落としが 1 件見つかった。

**大きさの調整**: 置き換え直後は線画が絵文字より一回り小さく見えた。絵文字は字送りいっぱいまで絵が詰まっているが、この 24 枠は中身が **約 72%** しか埋めていないため。1em ではなく **1.26em** を出すことにして、各所の `font-size` 指定を書き換えずに釣り合わせた。

**綴りの保証**: `CareDef.icon` は core の型なので、`IconName` に狭めると **core が ui に依存する**。層の向きを壊す代償が大きいため型では縛らず、`tests/icons.test.ts` で「設定表のアイコン名がすべて実在すること」「同じ画面に並ぶ絵が重複しないこと」を固定した。`iconOrText` は未知の名前を黙って文字として出すので、この網がないと綴り間違いが英単語として画面に出る（実際に出した）。

**絵柄そのものも 6 個描き直した**。最初に描いたものを 46px で並べて見たら、`medal` は鍵穴、`hatch` は帽子をかぶった人、`flame` は葉に見え、`ball` は `globe` と区別がつかなかった。`ball` は「まり」という比喩自体が円形アイコンと衝突しているのが原因だったので、3 度目で**紙ひこうき**に替えた（かざぐるまは線画にすると四弁の花になり、今度は `sprout` `leaf` `pot` と系統がぶつかった）。

**教訓**: 「型が通る」「テストが通る」は**表示の正しさを何も言っていない**。文字列を画面に出す種類の変更は、必ず撮って見る。そのうえで、目視で拾えない残りは機械的な走査で潰す。

---

## D-027. 顔の作り直し — 「引きで消える」を口の大きさで解こうとしていた

製品オーナーの方針により、目と口を作り直した。**これは以前のビジュアル批評の指示を一部反転させている。**

批評は「口が目に対して小さすぎ、引きで見ると消える」と指摘し、実装は口幅÷目幅の中央値を **1.99**、面として開いた口を **25.0%** まで引き上げていた。その結果が「大きな笑い口・人間の唇に見える下唇の帯・歯の面」で、**画面いっぱいの笑顔が並ぶ**状態になっていた。方針は「多様な顔なのに、どの個体もゲノモンらしく愛着を持てる」であり、これに反する。

| 指標 | 修正前 | 修正後 |
|---|---|---|
| 口幅 min / 中央 / max | 28.5 / **63.4** / **90.7** | 25.8 / **38.3** / **51.4** |
| 口幅 ÷ 白目幅 中央 / max | 1.99 / 10.20 | **1.21 / 4.73** |
| 面として開いた口 | **25.0%** | **14.5%** |
| 目 1 つの描画要素数 中央 | **11** | **6** |
| 目と口の間隔が負 | **6 体** | **2 体** |
| 白目幅（正味の目の大きさ） | 31.4 | **31.4（不変）** |

**引きでの可読性は幅ではなく線で取り直した。** 口の線の太さは `strokeW×1.2 → 1.15` とほぼ維持し、96px 等倍で撮った実ラスタで、口が最小の個体（幅 25.8 ＝ 96px で約 12px、線 1.6px）でも形が判別できることを確認した。**「小さいと見えない」の解は大きくすることではなかった。**

**虹彩の縦横比を新設した理由**: 白目が極端に縦長の『たまご』型で虹彩が真円だったため、上下に白が大量に残り**白目を剥いた人間の目**に見えていた（`WUYZ-S7QE` `3ZC7-B8YR`）。虹彩を 1.65 倍縦長にして解消。「人間の目に近すぎる形を避ける」は、目の大きさではなく**白目と虹彩の形が合っていないこと**が原因だった。

**への字を潜性から外した（リード判断）**: 方針は「小さな笑顔・ぽかん口・への字・無表情」を基本形と定めているが、`pout` は dominance 2 で上に立てるのが `none`（dominance 1）だけ。**実測 2%** で、基本形と呼べる数ではなかった。dominance を 3 にして `wavy`/`open` と対等にし（同値ヘテロは 50/50 の抽選）、weight を 12→15、そのぶん `smile` を 28→25 に下げた。

実測（4000 体）: にこり 42.6% / ちいさい 28.8% / なみなみ 9.8% / **への字 9.2%** / ぽかん 8.9% / なし 0.7%。にこりは下げてもなお最多で、「基本はにこり」の位置は変わっていない。**不機嫌な顔を増やすのが目的ではない**ので 9% 前後に留めた。希少度分布への影響も確認済み（交配集団の precious 2.9%・初期卵 0.6% で変化なし）。

**教訓**: 批評が挙げた症状（引きで消える）は本物でも、**そこから導いた処方（大きくする）が正しいとは限らない**。症状と原因を取り違えたまま数値目標にすると、目標は達成されて品質は下がる。

---

## D-028. 白目を減らす — 「減らす」と「無くす」は違う

製品オーナー方針「通常個体では白目の面積をかなり減らす／白目が大きく見える目は人間っぽさや不気味さが出る／基本は黒目主体の丸目・点目・小さく単純な虹彩／**白目がはっきり見える目は少数派に**／視線が強すぎない、やわらかく単純な目」。

**測り方**: `getBBox` では面積が出ないので、ブラウザに実際に描いた SVG の DOM に対し `SVGGeometryElement.isPointInFill()` で内外判定した。白目パスの bbox に 110×110 の格子を張り、「白目の内側か」「虹彩楕円の内側か」「上まぶたの塗り面の内側か」を判定する。指標は **`pVis` ＝ (白目 − 虹彩∪上まぶた) ÷ 白目**（＝画面で実際に白く見える割合）。幾何だけの比では、ねむたげのように上まぶたが白を隠す形で実感とずれる。ラスタの画素を数える方法は、白目の色が配色から作られるようになったため使えない。

| 成体 200 体 | 着手前 | 完了後 |
|---|---|---|
| pVis 中央値（全個体） | 0.428 | **0.206** |
| 白く見える面積 中央値 | 280 px² | **126 px²** |
| pVis ≥ 0.40 の個体 | 60.5% | **11.0%** |
| pVis ≥ 0.50 の個体 | 24.0% | **8.0%** |
| 白目を持たない個体 | 16.0% | **26.5%** |
| 点目 | 10% | **20.5%** |

**「やわらかく」は形だけでは作れなかった。** 虹彩を大きくするだけだと今度は「黒目が大きいだけの圧の強い顔」になる。色でも作った——瞳を `mix(pupil, iris, 0.3)` にして真っ黒を避け、白目を `mix('#fffdf8', body, 0.18)` にして純白（画面でいちばん強い白）を避け、ハイライトを小さく・低不透明度にした。

**`irisAsp` を 1 未満側にも開いた**。平たい目（ねむたげ rx1.2/ry0.5）で虹彩が `ry` に縛られ直径 0.95s の円になり、**左右に白が 0.725s ずつ残っていた**。D-027 で「縦長の白目に真円を入れると白目を剥いた人間の目になる」と書いたのと同じ問題の裏返しで、片側しか塞いでいなかった。

### ここで方針を取り違えかけた

描画側で白目を減らし切ったところ、**pVis ≥ 0.40 が 60.5% → 1.0% になり、「白目がはっきり見える目」という帯そのものが消えた**。方針は「少数派にする」であって「無くす」ではない。実装担当がこれを限界として正直に報告したので、リードが出現率側で直した。

`eyeShape` の『ぱっちり』(`wide`) weight **15 → 8**（dominance 4 は据え置き）。対立遺伝子比 15.8% に対し**発現は 24%** で、同 dominance の 3 種との抽選に半分勝ち下位 3 種には必ず勝つため、重みの割に出過ぎていた。減らした 7 は『まるめ』16→20 /『たまご』16→18 へ回した（方針が基本形に挙げる「黒目主体の丸目」を厚くする方向）。潜性側（このは・みかづき・ほしぞら）の相対的な珍しさは変えていない。

実測（4000 体）: まるめ 29.3% / たまご 25.6% / ねむたげ 21.6% / **ぱっちり 10.8%** / このは 6.1% / みかづき 5.7% / ほしぞら 0.9%。

そのうえで『ぱっちり』にだけ白目を戻した。**戻し幅は提案の 0.62 ではなく 0.68 を採用した。** 0.62 では `A8BC-G7N4` `5YXV-VB9B` で白が「縁」ではなく「面」として虹彩を囲み、『ぽかん』の開口と合わさって驚いた人間の顔に戻っていた（同一 seed で 0.62 / 0.68 / 0.72 を並べて目視）。0.72 では ≥0.50 が 0.5% まで落ちて帯がまた消える。

**`slit`（たてぼそ）を太くした**。虹彩が大きくなったぶん縦細瞳が目立ち、`A8JM-PBUW` はヤギ、`EZNE-7HFU` はヘビの目になっていた。`s*0.10 × s*0.46` → `s*0.17 × s*0.34`（縦横比 1:4.6 → 1:2）。識別性は保っている（同じ目で `round` の半径に対し幅 57%・高さ 113%、96px でも見分け可能）。副次的に、96px でほぼ消えていた細線が見えるようになった。

**残った制約（正直な記録）**: pVis ≥ 0.50 の帯は**現在 100% が『ぱっちり』**。着手前は ぱっちり／たまご／まるめ に散っていた。「ぱっちりだけが白目のはっきり見える珍しい目」という設計の必然的な帰結で、他の形では白目がはっきり見える個体は出ない。

**教訓**: 「A を減らす」という方針を数値目標に落とすと、**ゼロにするのが最適解**になってしまう。方針が「少数派として残す」なら、減らす方向の作業とは別に**残す仕組み**を用意しなければならない。ここでは描画（虹彩の大きさ）と遺伝（出現率）という別のレバーに分担させて両立させた。

---

## D-029. 瞳孔の廃止と、遺伝子座の意味を変えるという選択

製品オーナーが別のゲームの絵を参考に挙げ、「**瞳孔がいらないと思う。目はもう少し大きく**」と判断した。参考の絵を実際に描画して拡大したところ、目は「大きな円を体色で塗り、太い輪郭で囲み、中に中間色の面が 1 つ、小さなきらめきが 1 つ」で、**濃い点を持たない**。視線の焦点になる点が無いので、目が大きくても見つめてこない。

### 判断は 3 段階を並べて見てから行った

数値で議論しても決まらないので、`src/render/eyeTrial.ts` という**比較専用の一時スイッチ**を入れ、`sheet.html?eyes=cur|mid|ref` で同じ個体を 3 通り描いて並べた。既定値を「現状と同一」にして本番の絵は変えず、判定後に採用値を畳み込んでファイルごと削除した。

比較で分かったこと: **一律倍率は縦長の目を壊す**。`ref`（一律 1.42）では `8TQP-9GS9` `CHPU-CLM7` `NGPT-US8Q` の縦長の器が顔の上半分を占め、左右の目が繋がって**サングラス／ゴーグル**に見えた。参考の絵が成立していたのは**目が全部まん丸だったから**で、ゲノモンには 7 種の目の形がある。

→ 倍率を形ごとに分けた（`EYE_SIZE_K`）。まるめ 1.38 / ぱっちり 1.36 / ほしぞら 1.38 / たまご 1.18 / このは 1.18 / ねむたげ 1.0（上まぶたが器の 30% を覆うので広げても見える面が増えない）。

### 縦長は白目をなくした

製品オーナーの指示。縦長の器は虹彩を縦長にしてもなお上下に白が残り、大きくすると白の面積だけが増えて「白目を剥いた人間の目」に戻る。`oval` だけべた目にした（`isTallSolidEye`）。既存の**点目**（小さいインク一色の目）と混ざらないよう、大きさ・色・中身をすべて逆にし、描画側で点目を先に判定している。

`leaf`（このは）は指示に含まれていたが**縦長ではなかった**（rx 1.14 / ry 0.66 で横に細長い）。それでも両方描いて比べたところ、白目なしでは両端の尖りまで濃い一色で埋まった**細い切れ込み**になり、「少し不機嫌」を越えて「にらんでいる／傷」に寄った。白目つきのまま残した。

### 遺伝子座を捨てずに意味を変えた

瞳孔を廃止すると `pupil` 遺伝子座（6 対立遺伝子・うち 3 つが希少度に効く）が絵に出なくなり、交配で追う楽しみが 1 つ消える。**廃止ではなく意味を読み替えた**——「瞳孔の形」ではなく「**目の中の意匠**」。参考の絵も実はそうなっていて、ボタン目は 4 点、複眼は粒々と、瞳孔ではなく意匠として機能していた。

既存 6 種を意匠として描き直し（まるい＝無地の面／たてぼそ＝縦の帯／わっか＝二重リング／きらめき＝星／はなびら＝花形）、参考から 3 種を移植した。**対立遺伝子 ID は 1 つも消していない**——セーブは遺伝子型に ID を持っているので、消すと過去の個体が読めなくなる。追加は安全。

実測: ふくがん 7.6% / ボタン 7.8% / うずまき 2.4%。移植はコードを写さず、この作品の描画規則（SVG・名前付き乱数・単線と角丸）で描き直した。

### リードの見立てが実測で外れた（記録）

白目をなくした結果、**彩度の高い赤系の配色で「発光する切れ目」に見える**個体が出た（`W965-2FBS` 緑の体に真っ赤な縦長、`AMZQ-SLHM` 傷に見える一つ目）。リードは「彩度が効いている」と見立てて修正を指示したが、**実測では彩度を 78 → 52 に落としてもほとんど変わらなかった**。効いていたのは**明度**で、L29 まで落としてはじめて「濃いワインレッドの目」になった。

さらに、リードが「暗い赤なら大丈夫」の根拠に挙げた `V7Z8-9JC9` `4TZ6-SYT5` は**瞳が『つぶら』＝点目**で、そもそも `tallEyeFill()` を通っていなかった。**根拠にならない例を根拠にしていた。**

修正は色相距離による連続的な重みで、赤帯（H358 中心）だけ S・L の上下限を寄せる方式。色相は回していない（＋18〜25 回して茶に寄せた版は、赤い個体が「ただの茶色い目」になり個性が消えたので不採用）。寒色・茶系は重み 0 で**塗り色がビット単位で不変**。

**教訓**: 症状（赤い目が怖い）が本物でも、**原因の見立てが正しいとは限らない**。D-027 と同じ形の誤りで、あのときは「引きで消える」の処方を、今回は「発光する」の原因を取り違えた。**指示を出す側の仮説も実測で潰させること。**

---

## D-030. まつげ ── 「本数を増やして塊にする」から「1 本の帯」へ全面差し替え

2026-08-15 のセッション内で、まつげ (`lashes` 遺伝子座) の描画方式が **同じ日のうちに 2 回、正反対の方向へ** 変わった。あとから来る同種の指示に対する手がかりとして経緯を残す。

**版1（このコミットの下敷きになった未コミット差分）**: 「3〜4本の太いブレードが穏やかな扇」を、「密集した根元から鋭い棘が何本も広い角度で飛び出す、濃い塊」に差し替えた。本数を倍増、角度レンジを拡大、反りを弱めて直線的な棘にする方向。

**版2（今回・本判断）**: 製品オーナーから、版1とは**まったく別系統の参考イメージ**（1本の帯が目の上辺に沿って流れ、目尻の外で先が細りながら上向きに跳ね上がる、という単一の筆致）が渡された。参考は複数の候補（枝分かれの少ないもの・2〜3本に分岐するもの）を経て、**最終的に分岐のない1本の跳ねへ収束**していた。製品オーナー自身がその形を実在の個体の片目へ手で合わせ込んで確認しており、「これに合わせて」という指示だった。

版1の「棘の塊」方針を破棄し、`drawLashes`（`src/render/parts/face.ts`）を丸ごと書き直した。

- 複数本の毛の束 → **太さが変化する1本の帯**（`ribbonPath`、`src/render/svg.ts` に新設。キー点を Catmull-Rom で結び、各点に太さを持たせて法線方向へオフセットする汎用ヘルパー）。
- 付け根は目の実際の上まぶた輪郭 (`upperLidY`/`upperContourY`/`closedUpperY`/`solidUpperY` と同じ分岐) を複数点サンプルしてなぞり、目尻を過ぎたところで下にたわみ、先端で上に巻き返す。
- スタイル（ショート/ちょい長/ロング/サイド長め/上まつ毛み/おねむまつ毛）ごとに「なぞる範囲・しっぽの長さ・たわみ・巻き返し量・太さ」の6値だけを変え、構造は共通にした。下まつ毛は据え置き（参考が上まつ毛の形しか示していないため）。

**版1は同日に採用されたばかりだったが、差し戻した。** 製品オーナーの最新の参考画像・実個体への当てはめが優先度として上（版1のコメントが依拠していた「参考イメージ」が何だったかは、この時点のコードからは分からない）。**同じ遺伝子座のコードが1日で2回、逆方向に全面差し替えられている**ことは、次にこの座を触るエージェントが把握しておくべき事実として明記しておく。

### 「テストは通るが参考と似ていない」を 4 回踏んだ

最初の実装は型・テスト・自動検査をすべて通ったが、**拡大して参考画像と並べるまで似ていないことに気づかなかった**。目のところだけを切り出して大きく見る（Playwright の `clip` で顔の上半分を切る）ようにして初めて、次の4つが順に見えた。AGENTS.md が言う「テストが green でも合成された結果が意図通りとは限らない」の実例なので、症状と原因を残す。

| # | 症状 | 原因 | 対処 |
|---|---|---|---|
| 1 | 中央が団子状に膨らみ、眉毛／毛虫に見える | 帯が太すぎた | `width` を 0.30 → 0.20 前後へ。参考の帯は思ったより細い |
| 2 | 虹彩の上に厚くかぶって重い | 中心線をまぶたの上辺に置いていた | 中心線を幅の 6 割ほど上へ逃がす（`lift`）。まぶたの線の上に乗せる |
| 3 | しっぽが尻すぼみ／内へ巻いた小さなトゲになる | 跳ねを**横方向**で作ろうとしていた | 横は `lashRoom`（目尻〜体の輪郭の余白）で頭打ちになる。跳ねの主成分を**縦**へ移す（`dip` 浅く・`curl` 深く） |
| 4 | 目尻に V 字の切れ込みができ、本体としっぽが別部品に見える | 肩からしっぽの底へ一気に落としていた | 肩からゆるやかに continue させ、底を浅く取る |

とくに **3 は構造的な制約**で、次に同種の指示が来ても同じ壁に当たる。目は体の端に寄って配置されることが多く、目尻の外に使える横幅はたいてい狭い。「参考画像では長く横に伸びている」形をそのまま横で再現しようとすると必ず潰れる。**縦に逃がすこと。**

### 確認した範囲

- `npx tsc --noEmit` / `npx vitest run`（221件）/ `npx vite build`（`dist/` が `index.html` 系のみ）は通過。
- 自動検査 (`inspectModel`) は、版1（差し替え前）と版2（差し替え後）で**まったく同じ1件**（`starry`×`lower` の `face-outside-body`、既知の既存不良）以外に差がないことを、目の形 × まつげ種類の全組み合わせ（672通り）と完全ランダム3000体の両方で `git stash` を使い**新旧を同一条件で突き合わせて**確認した。目同士の重なり検査 (`eye-overlap`) も同様に新旧で完全一致（額に3つ目の目を持つ個体の配置起因で、まつげとは無関係）。
- 見た目は Playwright でスクリーンショットを撮り、複数のまつげスタイル・目の形（まるめ・ぱっちり・みかづき 等）を実際にズームして目視確認した（自己申告で済ませていない）。

**この版も採用に至らなかった。** 続きは D-031。

---

## D-031. まつげ ── 手続き的な作図をやめ、原画スプライトを使う

D-030 の版に対する製品オーナーの評価は「やっっぱりまだまつげ微妙だな、、、ちょっとは良くなったけど」。同じ日に 3 度目の作り直しになるが、今度は **近似をやめた**。製品オーナーが 8 種のまつげを描いた SVG（`art/eyelashes_sprite.svg`）を渡してきたので、それをそのまま素材として使う。

### 原画を見て初めて分かった、数式では出せていなかった要素

拡大して並べたところ、手続き的な作図が何を落としていたかがはっきりした。

1. **先端の枝分かれ（プロング）が 2〜3 本ある。** D-030 の「太さの変わる 1 本の帯」は、構造上どうやっても 1 本にしかならない。ここが「まだ微妙」の主因だったとみられる。
2. **まつげの上に白いつやの点が乗る。** 意匠として描かれていた。数式側では存在すら認識していなかった。

**教訓**: 参考画像を「雰囲気の参考」として読んで数式に落とすと、絵の構成要素そのものを取りこぼす。**原画があるなら原画を使う。** 3 周ぶんの調律より、素材を正しく貼るほうが早くて正確だった。

### 実装

- 原画は `art/eyelashes_sprite.svg`（リポジトリに置いて由来を残す）。
- `node tools/genLashSprites.mjs` で `src/render/parts/lashSprites.ts` を生成する。**生成物は手で編集しない。**
- 原画は 1px 刻みの直線ポリラインで 63,698 文字あり、そのままでは 100 体分の DOM に流せない。Douglas-Peucker で間引いて **15.9%** まで落とした（誤差は幅の 0.13%＝拡大表示でも 1px 未満。実測でビルドは +10KB / gzip +3KB）。
- 8 種のうち 7 種が既存の対立遺伝子とそのまま 1 対 1 で対応した（`01_short`→ショート … `08_sleepy`→おねむまつ毛）。**`07_droopy` だけが余り**。対立遺伝子を増やすのは製品オーナーの判断なので、いまはカタログに足していない（＝描かれない）。
- 配置は `LASH_PLACEMENT`（`face.ts`）で種類ごとに 5 つの数値だけを決める。原画は縦横同じ倍率で拡大する（別々に伸ばすと縦長・平たい目で手描きの線が歪む）。

### ここでも 3 回、実際に描いて直した

| 症状 | 原因 | 対処 |
|---|---|---|
| 枝分かれが目の上に乗ってしまう | まつげ **全体の幅** を目の幅に合わせていた | 原画は「本体が目をまたぎ、枝分かれは目尻からはみ出す」構図。**本体の終わり**が目尻に来るよう合わせる（`outerAt`） |
| 目の上半分が黒い「ヘルメット」になる | 上まぶたインクの下辺にまつげを載せていた | 目には既に太い上まぶたが描かれている。原画のまつげは **まぶたの線の上に乗る** 絵なので、器の輪郭の上端を基準にする |
| まつげが目から上へ浮いて眉毛のように分離する | **原画は 1 枚の硬い絵**で、目の丸みに追従できていなかった | 各点をまぶたの曲線ぶん縦にずらして原画をしならせる（下記） |

2 つめは D-030 で消した `upperLidY()` を使っていたことが原因で、この修正でこの関数は完全に不要になった（`ribbonPath()` ともども削除済み）。

### 「目から上方向に乖離している個体が散見される」（製品オーナー指摘）への対処

原画の弧は、目のドームよりずっと浅い。原画の下辺は中央から端まで幅の 13% ほどしか下がらないのに対し、目のふちは半幅ぶん進む間に `ry` まるごと下がる。中央で合わせると **両脇が上へ浮く**。縦長の目（たまご）や閉じ目（みかづき）ほど `ry / rx` が大きいので差が開き、まつげが眉毛のように分離して見えていた。

縦だけ引き伸ばすと手描きの線が歪むので、代わりに **各点をまぶたの曲線ぶんだけ縦にずらす**（原画をしならせる）。変換行列では曲線に沿ったしなりを表せないので、パスを点列に戻して 1 点ずつ計算している（原画は間引き済みで 1 枚 100〜300 点なので負荷は小さい）。

ここでも 2 段階の失敗を経ている。**どちらも「直したつもりが別の個体を壊す」型**で、1 つの目の形だけを見て判断していたら気づけなかった。

1. **目の曲がりをそのまま全部与えた** → 原画自身の反りと足し合わさって反りすぎ、それまで問題の無かった *まるめ* の目で、まつげが左右へ回り込んで「囲い」になった。
   → 移すのは（目の曲がり）−（原画自身の反り）の **差分だけ** にした。両者の曲率が合っていれば差は 0 で原画は無変形、目のほうが急なぶんだけ反りが補われる。そのために生成側へ原画の反りの列（`profTop` / `profBot`）を出させている。
2. **差分でもまだ縦長の目には大きすぎた** → まつげが目の側面まで回り込んで抱え込む。
   → まつげ自身の大きさに対する割合で頭打ちにし、`tanh` でなめらかに飽和させた（単純に切るとそこだけ平らな折れができる）。

### `07_droopy` を採用した

製品オーナーの判断で、余っていた 8 種目を `droopy`（たれまつげ）として `CAT_LOCI` に追加した。weight 4。まつげを持つ個体の割合は 34% → 36.5% に少し上がる。

### 確認した範囲（更新）

- 目の形 8 種 × まつげ 8 種 × `full`/`lite` の **768 通り** で自動検査・SVG の決定論性・NaN 混入を確認。検出は 2 件のみで、どちらも **まつげとは無関係**であることを `lashes: 'none'` との比較で確認済み（`starry/lower` の既知分と、尾が描画領域を出る既存不良。後者は別タスクとして切り出した）。
- ランダム 3000 体の自動検査はまつげ導入前のベースラインと **完全に同一**。
- 目の形 8 種それぞれについて、まつげ全種類を並べたシートを実際に目視。とくに乖離が出ていた たまご・みかづき・したりめ は拡大して確認した。

### 確認した範囲

- `npx tsc --noEmit` / `npx vitest run`（221件）/ `npx vite build` は通過。`dist/` は `index.html` 系のみで、`art/` と `tools/` はバンドルに入らない（生成物の TypeScript だけが入る）。
- 自動検査は D-030 以前のベースラインと **完全に同一**（ランダム3000体で3件・目の形×まつげ672通りで1件。いずれも既知の既存分）。SVG の決定論性・NaN 混入も 672 通りで確認。
- 明るい／暗いテーマ、`full`／`lite`、96px の一覧、拡大表示をそれぞれ目視確認。

---

## D-032. まつげ ── 「まぶたの上に載せる」から「まぶたのインクへ重ねる」へ

D-031 の実装をレビューに掛けたところ、**8 つの目の形のうち 4 つで破綻していた**。
D-031 の完了条件（型・221 件のテスト・ビルド・e2e・自動検査ベースライン）は
すべて通過していたのに、である。

### 何が起きていたか（実際に見て確認した症状）

| 症状 | 出ていた条件 |
|---|---|
| まつげが目から離れて浮き、**黒い動物**に見える | みかづき・したりめ × ほぼ全種 |
| まぶたと合わさって上半分が**黒い塊**になる | ねむたげ・このは × ほぼ全種 |
| とげが目の中へ垂れて**檻**に見える | 全形 × `sleepy` |
| **虫の脚**に見える | 全形 × `lower` |
| 点目で本体が溶け、**白い点だけ**が汚れとして残る | `pupil: bead` × 全種 |
| 元気のない個体でまつげがまぶたに飲み込まれて消える | `mood.droop` が高い個体 |

### 根本原因は 1 つ ── 基準線の選び方

D-031 は「まぶたの下辺に合わせると黒いヘルメットになる」という観察から、
まつげを **目の外側の輪郭** に載せた。つまりまつげは
**目に一切重ならず、地肌の上に置かれた別の絵** になっていた。その結果:

- 目の弧が急な形（みかづき・したりめ）では追従しきれず **浮く**
- まぶたのインクが元から太い形（ねむたげ・このは）では、
  インクの塊とまつげの塊が **積み上がって** 黒くなる

浮きと塊化は別々の不良に見えていたが、**同じ原因の裏表**だった。

正しいのは「まぶたの線とまつげがひとつながりの塊になり、その**外側の輪郭が
まつげの形**になる」置きかた。そのためにはまつげの下辺をインクの下辺に合わせ、
**インクへ食い込ませる**（`LASH_PLACEMENT.sink`）。こうすると黒い面積は
「まぶた＋まつげ」ではなく「まつげ」だけになり、浮きも塊化も同時に消える。

D-031 が消した `upperLidY()` を、この目的で復活させている。

### しなりの上限を「量」から「傾き」へ変えた

D-031 は、しなり量そのものに `tanh` で上限を置いていた。しかしこれでは

- 弧が急な目（みかづき）… 追従しきれず **浮く**
- 縦長の目（たまご）… 端で輪郭が垂直に落ちるので **回り込む（囲い）**

を同時には解決できない。上限を上げれば囲いになり、下げれば浮く。
このトレードオフの正体は「しなりの大きさ」ではなく **傾き** だった。

- まぶたに貼り付いて見えるか → 追従できているか
- 目を囲って見えるか → 横から下へ **どれだけ急に** 降りるか

なので制限すべきは傾きのほう（`slopeLimited` / `LASH_MAX_SLOPE = 1.15`）。
みかづきの弧は中央付近がゆるやかなので最後まで追従でき、たまごの端
（垂直に落ちる）だけが頭打ちになる。

### 白いつやの点は描かないことにした

D-031 が「数式では出せていなかった要素」として喜んだ意匠だが、**実寸では
害のほうが大きい**と分かったので描画をやめた。

- ゲームでの実寸はまつげの幅 30px 前後で、点は 1px 未満。96px の一覧では
  `full` と `lite` に見た目の差が出ない（＝**そもそも見えていない**）。
- 拡大表示では、黒いシルエットの中の白い点が **動物の目** に読める。
  みかづき・したりめが「目の上を魚が泳いでいる」絵になっていた主因。
- 点目（`bead`）では目自身のハイライトに加えて **2 つめのハイライト** が
  浮き、傷か汚れに見える。

原画と `lashSprites.ts` の `highlight` はそのまま残してあるので、
戻したくなったら `drawLashes` で描くだけでよい。

### 下まつげの背丈は「目の高さ」で抑える

倍率を幅だけから決めると、横長の目（このは・ねむたげ）では `ry` が小さいのに
まつげが目の幅なりに大きくなる。下まつげの原画は「帯＋下向きのとげ」なので、
背が高すぎると帯が目の中へ入って **歯** に見え、逆に沈めて隠すと今度は
とげの先だけが飛び飛びに覗いて **点線** になる。`maxH` で高さそのものを
目の高さの 32% に抑えるのが正解だった。

あわせて `lowerContourY()` を `whiteShape()` の実際の曲線に合わせ直した。
2 次式の近似が実際の下辺よりかなり内側だった（ねむたげ 0.65ry / 実際 0.91ry、
このは 0.82ry / 実際 0.94ry）ため、下まつげの付け根が目の中に入っていた。

### 自動検査がまつげを見ていなかった

`inspectModel` のはみ出し検査は `bbox` の周囲 8 点しか見ておらず、目の `bbox`
は白目の実寸のままだったので、**まつげはどの検査にも入っていなかった**。
実測で したりめ＋`long` は宣言 bbox より 11.1 上（目の高さ 12.5）まで
描かれていたのに検査は 0 件だった。

かといって bbox にまつげを足すのは誤りで、矩形の角という **絵の無い場所** を
検査してしまう（成体 300 体中 43 件の誤検出が出た）。**輪郭上の点**
（`RenderPart.probes`）で渡すのが正しい。

これを入れたら、まつげが体の輪郭を突き抜けている個体が成体 300 体中 31 体
見つかった（顔は体でクリップされるので、はみ出した先は平らに切り落とされる）。
`lashRoom` が目の高さ ±0.7ry の 3 点でしか横幅を見ておらず、まつげが目の
**上** へ伸びるぶんを見落としていたため。`ctx.shape.outsideAt`（`inspectModel`
と同じ折れ線）で判定し、収まるまで原画の形を保ったまま縮めるようにした。
実測 1500 体中、まつげが描かれなくなった個体は 0 件。

### 教訓

- **「テストが通った」は「正しく描けた」ではない。** D-031 は 221 件すべてに
  通っていた。既存のテストは「SVG に差分があるか」しか見ておらず、
  *どこに* 描かれたかを誰も検査していなかった。
- **対照（まつげ無し）を並べずに黒い塊を評価しない。** 「まぶたのインクなのか
  まつげなのか」は並べないと切り分けられず、原因の取り違えに直結する。
- **1 つの目の形だけを見て判断しない。** 浮きと塊化のように、
  片方を直すと片方が悪化する組み合わせがある。
- 回帰テストは `tests/lashes.test.ts` に置いた。**基準線を外周へ戻す**
  変更と、**輪郭に収める処理を外す** 変更で、実際に落ちることを確認済み。

### 追記: 白目を持たない目で、まつげが消えていた（製品オーナーの再指摘）

上の修正後、製品オーナーから「うまくマッチしていない、目と離れている個体もいる。
別添 svg ほどのクオリティもない」という指摘を受けた（`QHJS-K7MY` `S6AG-YGBX`
`7QL5-ARVD`）。3 体とも **たまご（べた目）＋濃い配色**だった。

**私の検証が緑の個体 1 体に偏っていたのが原因。** 配色を変えて確かめていなかった。

#### 測ってみたら一目瞭然だった

まつげはインク色で描いている。その明度差（1.0 で同色）:

| 個体 | ink 対 irisDark | ink 対 地肌 |
|---|---|---|
| QHJS-K7MY (dusk) | **1.23** | 3.98 |
| S6AG-YGBX (frost) | **1.20** | 6.22 |
| 7QL5-ARVD (bloom) | **1.80** | 6.66 |

たまご（`isTallSolidEye`）と つぶら（`isSolidEye`）は **白目を持たない**。
器の中はまるごと濃い虹彩なので、`sink: 0.30ry` で沈めた部分は
**色が同じで完全に見えなくなる**。残るのは器からはみ出した細い毛だけ ＝
「目から離れて浮いた数本の毛」。原画の太い帯はどこにも見えていなかった。

#### 3 つ直した

1. **基準線**: たまごは まぶたの線（器の内側にある）ではなく **外周リング**
   （`upperContourY + strokeW * 0.41`）に載せる。まつげ本体が地肌の上
   （コントラスト 4〜6.7:1）に出る。
2. **沈める量**: 白目が無い目では `min(sink * ry, strokeW * 0.35)` で頭打ち。
   リングに重なるぶんだけにして、濃い虹彩の中へ潜らせない。
3. **細い縁**: 白目が無い目に限り、まつげへ `mix(ink, body, 0.5)` の細い縁
   （`strokeThin * 0.42`）を付ける。器の上では明るい隙間として帯の形が浮かび、
   地肌の上では同色なので何も足されない。
   *地肌の色をそのまま使うと明るすぎて「色つきの縁取りシール」に見えた* ので、
   インク寄りへ寄せた中間色にしている。

白目のある 6 形（まるめ・ぱっちり・ねむたげ・このは・ほしぞら・閉じ目）の
描画は **一切変えていない**（`hasSclera` で分岐）。

#### 教訓（前の教訓の上書き）

- **1 個体・1 配色で見た目を検証してはいけない。** 「目の形 8 種 × まつげ 8 種」を
  総当たりしても、配色が 1 つなら**色に依存する不良は 1 つも見つからない**。
  インクと虹彩の明度差はパレットごとに 1.2〜3.0 と倍以上ばらつく。
- 見た目の検証には **コントラストの実測** を混ぜる。「見えるはず」は当てにならない。

### 追記2: 「目とまつげのカーブが合っていない」（指摘 6 件）の原因

`upperContourY` は `|x| ≧ rx` で **0（目の縦中心）** を返す。まつげは目尻の外へも
伸びるので、そこで基準線が `-ry` から `0` へ **崖のように落ちる**。
`slopeLimited` はこの崖をならすのに傾きの許容量を使い切ってしまい、肝心の
「目のドームに沿う」ぶんが残らなかった。まるめ・ほしぞらでまつげが目の上に
浮いて見えていたのはこれ。

- 目の幅の外では **ふちの高さを保つ**（`edgeHold`）。閉じ目の弧は `rx * 1.08` まで。
- 崖が無くなったので `LASH_MAX_SLOPE` を 1.15 → 2.4 に上げられた。
  まるめの目はふち近くで傾き 2 前後まで上がるので、1.15 では途中で追従をやめていた。
  縦長の目（たまご）は引き続きここで頭打ちになり、側面へ回り込まない。

閉じ目（みかづき・したりめ）は別の問題だった。目のすべてが **細い弧 1 本**
（線幅 strokeW×1.15）なのに、原画の帯はその 4 倍ほど厚い。そのまま載せると
弧を置き換えてしまい、両端の枝分かれだけが外へ出て「脚の生えた別の生きもの」に
見える。高さを `ry` の 1.3 倍までに抑え、弧に添う細いまつげにした。

### 追記3: おねむまつげは「下まつげ」

`08_sleepy` は「帯＋下向きのとげ」の構図で、上まぶたに載せるととげが目へ垂れて
檻に見え、引き上げると横棒になる。製品オーナーは目の形を変えながら **6 個体で
同じ補正**（いずれも下方向、+11〜+41）をしていた。上まぶたではなく
**下まぶた** に付けるのが正解だった。

### 追記4: 装飾の「横へ逃がす」先が、逃げる相手そのものだった

植物は頭頂が混むと `halfAt` の 50% へ横へ逃がしていた。ところが頭頂を占める
相手も同じ帯にいる — 角 52%、触角 50%。**逃がした先が相手そのもの**。実測:

| seed | 植物の x | 相手の x |
|---|---|---|
| RJ3P-9UAN | 22.3 | 角 23.1 |
| MLA9-UEJW | 15.4 | 角 16.0 |
| LD9V-5F8V | 17.3 | 角 18.0 |
| AE68-XH52 | 31.2 | 触角 31.2 |
| HB8C-QAKE | 30.8 | 触角 30.8 |

製品オーナーは 5 個体すべてを手で **中央（x ≒ 0）** へ戻していた。角も触角も
左右対に立つので中央は必ず空いている（両方ある個体は `veryCrowded` で植物を
休ませてある）。素直に中央へ置くのが正解。

**教訓**: 「重ならないように逃がす」処理を書くときは、逃がす先に何がいるかを
**実際の座標で確かめる**。この 5 件はどれも 1 単位も離れていなかった。

---

## D-033. 「可愛さ」ではなく「壊れている」を機械で拾う ── 装飾の根元の検査

製品オーナーの「可愛さを AI を介さずに自動テストできるようになりたい」から。

### 出発点: 今回の指摘 13 件を「測れたか」で仕分けた

可愛さの判定は要らなかった。**13 件のうち 10 件は測れる不良**だった。
実際、この一連の修正でバグを特定できたのは「よく見た」からではなく
**測った**から — `1.20`（インクと虹彩の明度比）、`0.8`（植物と角の距離）、
`22.3 対 23.1`（座標）。緑の個体を何度眺めても、濃い配色の不良には気づけなかった。

既存の検査は 10 項目あったが、**重なりを見ていたのは目どうしだけ**で、
装飾どうしは見ていなかった。だから植物と角が 0.8 しか離れていなくても素通りした。

### 捨てた案: 装飾どうしの bbox の重なり

最初はこれで判定した。しかし角や触角は曲がった細い器官で、外接矩形が
空白だらけになる。隣り合っているだけの健全な個体まで重なりになり、
**成体 3000 体中 998 件（33%）の誤検出**。まつげのときの「矩形の角」問題と
同じ構図だった。矩形は細い器官の代用にならない。

### 採った案: 根元の **横方向** の距離

頭頂の装飾はどれも上へ伸びるので、「同じ列から生えている」なら必ずぶつかる。
`PartOut.roots`（その器官が体から生えている点。左右 2 本なら 2 つ）を
植物・角・触角に持たせ、別々の装飾の根元が横に `ROOT_MIN_GAP = 8` 以上
離れていることを要求する。

### ここでも一度、検査が本物のバグを捕まえられなかった

最初は根元どうしの **ユークリッド距離** で見ていた。ところが不良を出していた
旧実装は、植物を横へ逃がすと同時に **下へも 0.14×H（約 15）ずらして** いた。
そのため横が 0.8 しか離れていなくても距離は 15 を超え、旧実装に戻して確かめたら
**検出 0 件**だった。横方向だけで測るように直して、旧実装で 204 件・現行で
11 件（0.37%）になった。

**教訓**: 新しい検査を書いたら、**必ず「直す前のコード」に戻して落ちることを
確かめる**。落ちない検査は、無いよりたちが悪い（「検査済み」で覆い隠す）。
これは `tests/inspect.test.ts` に合成モデルの単体テストとして固定した。

### 残った 11 件は本物

頭が細く、角の根元が中心から 7.4 しか離れていない個体。植物と角が両方は
入らない。`SCAN-409` `SCAN-416` では葉とサンゴ角が混ざって塊になっている。
**これを消すには「頭が細い個体では植物を休ませる」という判断が要る**ので、
機械では決めない。検査が拾って人が決める、という分担にする。

### この検査で測れないもの

- 好み（おねむを上に付けるか下に付けるか）は測れない。実例 6 件を見て初めて分かった
- 「魚に見える」も直接は測れない。引き金だった「暗い塊の中の孤立した明点」は測れる

**指標は不良を捕まえるワナであって、可愛さの点数ではない。** ここを混ぜると
指標を満たすだけの退屈な絵に寄る。`27ST-9MSA` のように製品オーナーが
「今のままでうまく行っている」と言った個体を、機械が「悪い」と判定する
可能性は常にある。


---

## D-034. Cards Lab ── 本編に入れる前に、カードを「見て決める」場を作る

トレーディングカードをゲーム本編へ組み込む前に、`lab.html` の第 3 タブとして
**カードの研究環境だけ** を先に作った。鑑定所・発行・所持・アルバム・
マーケットは一切実装していない。GameState もセーブ形式も変えていない。

### なぜ本編より先に研究環境なのか

カードの価値は「情報が正しく載っていること」ではなく「見て集めたくなること」
で決まる。これは仕様書では決められず、実物を並べて人が選ぶしかない。
先に発行の仕組みを作ってしまうと、あとから版面を変えるたびに
セーブ形式の互換を気にすることになる。**見た目が固まるまで、
カードはどこにも保存されない状態にしておく。**

### 外部ライブラリ: `@kongyo2/cards-css`（devDependency）

tilt / glare / shine / depth / foil マスク / パレット / 手続き的テクスチャ /
レイヤーを持つフレームワーク非依存のライブラリ。**本編では使わないので
`devDependencies`** に入れてある（`vite.config.ts` の入口は `index.html`
だけなので、本番ビルドには 1 バイトも入らない。ビルド後に `dist/` へ
`holo-card` の文字列が出ないことを確認済み）。

`createHoloCard()` ではなく `attachHoloCard()` を使う。前者は
「1 枚の画像 + overlay」を前提に DOM を組み立てるが、ゲノモンのカードは
画像ではなく **その場で組み立てた SVG と文字組み** なので、版面は
こちらが完全に握る必要がある。ライブラリが要求する骨格
（`.holo-card__rotator` 以下）だけ自前で作り、挙動を後付けする。

### 決定論（AGENTS.md §1 の延長）

カードに出る値（証明書番号・通し番号・血統名・展示実績・foil の
テクスチャ seed）は、すべて `new Rng(seed).stream('card:...')` から作る。
`Math.random()` も `Date.now()` も使っていないので、同じ個体の Prism カードは
いつ開いても同じ粒子配置になる。`tests/cards.test.ts` が 2 回導出して
完全一致することを機械的に確かめている。

模様・質感・配色・希少度・保因形質は **Phenotype の実データ**。
親・展示実績・世代・鑑定日は、まだゲーム側に無いのでダミー（決定論）。

### 失敗 1: 「マスクは黒く塗れば隠れる」は誤り

foil を部位ごとに掛けるため、SVG のマスクを白＝出す／黒＝隠す のつもりで
描いた。ところが CSS の `mask-image` は `mask-mode: match-source` が既定で、
**画像を渡すと輝度ではなくアルファで切り抜く**。黒も白も不透明なので
マスクは何も隠さず、Prism がカード全面を洗い流して
**ゲノモンの体色と模様が完全に消えた**（うしがら模様の紫の個体が、
淡い虹色の塊になった）。

直しかた: 描いた所＝箔が出る／透明な所＝箔が出ない で組み直す。
穴を開けたいときは SVG の `<mask>` でアルファを落とす。

さらに、暗い Collector では **アルファ 10% でも足りなかった**。箔は
`color-dodge` で合成されるので、暗い地の上ではわずかな不透明度でも
明るく焼き付き、Gold と Aurora がカード全面を 1 色に塗り潰した。
中心付近をほぼ 0 まで落として解決している。

### 失敗 2: `<rect width="1">` が消える

QR 風プレースホルダを `<rect>` の格子で描いたら、真っ白の箱になった。
ライブラリの CSS は `.holo-card__content *` で全子孫に `width:auto` を
掛ける。SVG の `width` は CSS から上書きできる幾何プロパティなので、
`auto` になった矩形は幅 0 になる。**カードの中に置く SVG は
`<path>` で描く**（`d` は CSS から触られない）。

### 失敗 3: 比較の 6 枚が 6 枚とも同じ絵に見えた

ライブラリは `--card-opacity` を 0 から始め、ポインタが乗ったときだけ
箔を出す。Showcase の 1 枚ならそれでよいが、Finish 比較では
**触らないと違いが分からない = 比較にならない**。しかも interactive な
カードは休止中も毎フレームの結果をインラインスタイルで書くので、
CSS から静止時の値を与えられない。

比較（medium）と一覧（lite）は `interactive: false` にした。すると
ライブラリは静止時にインライン変数を捨てて純 CSS のフォールバックへ譲るので、
`cardStyles.ts` が固定のポインタ位置を与えれば 6 枚が最初から別々の箔で並ぶ。
ホバー時はライブラリ内蔵の CSS が滑らかに持ち上げる。**軽くなった上に
比較として正しくなった。**

### 3 案の分担

| 案 | 方向 | 情報量 | 箔 |
|---|---|---|---|
| Certified | 鑑定証・グレーディング。アイボリー、金の細箔、大きな Grade | 中 | 枠・ロゴ・Grade・罫線だけ（0.46 倍） |
| Natural History | 自然史標本・血統証明書。パーチメント、罫線、系統図 | 最多 | 罫線と紋章だけ（0.38 倍） |
| Collector | chase card。ほぼ全面がゲノモン、深い黒 | 最少 | 個体の周りを抜いた上で強め（0.70 倍） |

**主役は個体**という一点だけは 3 案とも動かさない。個体の上に箔は乗せない。

### 保存キーを分けた

Visual Lab は `genomon.dev.prefs.v1` を正本にしている。Cards Lab が同じキーへ
書くと、片方を触るたびにもう片方の seed や段階が飛ぶ。
`genomon.dev.cardprefs.v1` / `genomon.dev.cardsaved.v1` を新設し、
**`cardStore.ts` からは `PREFS_KEY` を import しない**ことで、
同じキーへ書く経路そのものを作らないようにしてある。
書き込みは今までどおり `labStore.ts` の `putRaw`（`genomon.dev.` 接頭辞の
実行時検査）を通る。

### 残っている問題

`VISUAL_QA.md` の「Cards Lab 初回レビュー」に列挙した。要点だけ書くと、
Certified の中段（Grade 行）に空白が多い、Collector の背景が個体の色に
依らず似た印象になる、日本語と英字の混植が行によって重心がずれる、の 3 点。


---

## D-035. Cards Lab をスマホで使えるようにする ── 「次から次に」を成立させる

製品オーナーからの指摘:「スマホでも見やすく、直感的に操作できて、
簡単に再生成を押してたくさんカードを次から次に出せるように」。

カードは手に持って眺めるものなので、**美術判断がスマホでできないと
研究環境として片手落ち**だった。実装当初は PC の 2 段組をそのまま
1 列に畳んだだけで、次の 3 つが致命的だった。

### 直した 3 つ

**1. カードが画面に出てこなかった。**
設定パネル 6 枚が先に並び、カードを見るには毎回スクロールが要った。
DOM の並びを「見る場が先」に変え、PC 側は CSS の `order` で元に戻す。
設定は `<details>` に入れて畳む。

> **落とし穴**: `order` を PC 側に書き忘れると、設定パネルが
> `minmax(0,1fr)` の列へ落ちて **カードが 420px の列に潰される**。
> DOM 順を変えたら、両方の幅で必ず目視すること（実際に一度潰した）。

**2. 「つぎのカード」が遠く、しかも重かった。**
- 画面下に **デッキバー** を固定した（戻る／つぎのカード／版面／箔／♥保存）。
  親指の届く範囲だけで、めくる・変える・残すが完結する。
- 左右スワイプでもめくれる。**マウスのドラッグでは効かせない** ——
  ポインタを寄せると箔が動くのが Showcase の見どころなので、
  眺めるつもりの動きでカードが変わってはいけない。
- めくった seed を履歴に積み、「さっきの子が良かった」に戻れるようにした。
  たくさん出せても戻れないなら、たくさん出す意味がない。
- **1 タップの仕事量を減らした。** 以前は 1 回の再生成で Showcase 1 枚 +
  比較 9 枚 + 一覧 8〜30 枚を作り直していた。狭い画面では比較と一覧を
  畳んでおき **開くまで作らない**。開いていても重い所は 220ms の
  デバウンスに逃がし、連打しても最後の 1 回にまとめる。
  実測で 5 連打が 1.1 秒（Playwright の操作込み）。

**3. カードの下端がデッキバーの裏に 29px 潜っていた。**
横幅（`94vw`）だけで大きさを決めていたため。**高さからも上限を掛ける**:

```css
width: min(94vw, var(--cards-showcase-w), calc((100svh - 274px) * 0.716));
```

引いている 274px はヘッダ・台紙の余白・説明文・デッキバーの合計。
`vh` ではなく `svh` を使うのは、スマホのアドレスバーの伸縮で
`100vh` がずれるため（`@supports` でフォールバックも置いてある）。
説明文も 3 行に収まるよう、seed の行は 1 行に省略する。

これは目分量では気づけなかった。**実機幅で各要素の矩形を測って初めて
数字が出た**ので、`e2e/06-cards-lab.spec.ts` に
「カードの下端 ≦ デッキバーの上端」を回帰テストとして固定してある。

### ついでに直した: ヘッダのタブが 2 行になっていた

`labStyles.ts` の狭い画面用の指定が `grid-template-columns:1fr 1fr` で、
タブが 3 つに増えた時点で 3 つ目が折り返し、ヘッダが 134px を占めていた。
3 列にして 88px。**Cards Lab の都合だが、直す場所は共有の `labStyles.ts`**
（ヘッダは 3 タブ共通の部品なので、カード側の CSS から触るべきではない）。

### 判断: 小さくしてでも 1 枚を収める

高さの上限を掛けると、カード幅は 369px → 324px に縮む。
それでも収めるほうを選んだのは、**判断のためにスクロールが要る時点で
「次から次に」が成立しない**から。1 枚を丸ごと見て、親指で次へ、が
途切れないことを幅より優先する。


---

## D-036. スマホ幅の再点検 ── 「測ったつもり」で見落としていたもの

D-035 のあと、製品オーナーから「もう一度、スマホ幅にちゃんと合っているか
確認して」と言われて **320 / 360 / 375 / 393 / 428px と横向きを機械的に
測り直した**。1 端末（393px）でしか見ていなかったので、3 つ見落としていた。

### 見落とし 1: 画面より広いのに、横スクロールが出ないので気づけなかった

`.cards-right` を `display:grid` にしていたが `grid-template-columns` を
書いていなかった。暗黙のカラムは `auto`＝**max-content** で決まるので、
中に横並びの帯（比較の 6 枚）を置いた時点で **列が 380px に固定**された。

320px の画面では 60px ぶん右側が切れる。ところが `body` に
`overflow-x:hidden` が効いているため、**`scrollWidth === clientWidth` は
成立してしまう**。D-035 で足した「横スクロールしない」テストは、
この状態を通していた。

- 直し: `.cards-right{grid-template-columns:minmax(0,1fr)}`
- テストも直した: `scrollWidth` ではなく **要素の矩形**が画面をはみ出して
  いないかを見る（`e2e/06-cards-lab.spec.ts`）。
  意図して横スクロールさせる帯（`.cards-strip` / `.cards-gallery`）だけ除外する。

> **教訓**: `overflow:hidden` がある所で `scrollWidth` を見ても、はみ出しは
> 検出できない。「隠れているだけ」と「収まっている」は別。

### 見落とし 2: 320px でデッキバーが 2 行に折り返していた

5 つのボタンを 1 段に並べると、320px では「つぎのカード」が 62px まで
潰れて文字が 2 行になっていた。**主役がいちばん小さい**という本末転倒。

360px 以下では 2 段に分け、**最下段（親指にいちばん近い所）を
「つぎのカード」の全幅**にした。デッキの高さは幅で変わるので
`--cards-deck-h` にまとめ、余白とカードの高さ上限をそれに追従させる。

### 見落とし 3: 横向きにするとカードが 83px に潰れた

カードの大きさを「高さから引き算」で決めていたため、横向き（高さ 390px）で
`(390 - 274) × 0.716 = 83px` になった。**縦長のカードを横向きの画面へ
丸ごと収めるのは物理的に無理**なので、ここだけは「収める」をあきらめる:

- 高さ由来の上限に `max(200px, …)` の下限を付ける（潰れるくらいなら
  スクロールさせる）
- さらに横向き（`max-height:520px`）では幅から決め直し（`min(40vw,…,340px)`）、
  デッキと説明文を薄くして見えている面積をカードに回す

### ついでに: チェックボックスが 13px 角だった

`箔の 3D 押し出し` などの 3 つ。箱を 22px にしたうえで、
**ラベル全体を 44px 高のタップ対象**にした（指は箱ではなく文字を押す）。

### 3 度目の教訓: CARD_CSS はテンプレートリテラルの中の CSS である

この作業中にも 2 回踏んだ。コメントに **バッククォート** を書いて文字列が
途中で終わり、ビルドが止まった。以前は `content` の CSS エスケープが
二重に潰れて **制御文字**が焼き込まれ、折りたたみの三角印が文字化けした
（型検査もビルドも通ってしまう種類の事故）。

`tests/cards.test.ts` に、CARD_CSS へ制御文字が入っていないこと・
波かっこの対応が取れていること・スマホ用の要（`--cards-deck-h`、
`.cards-right` の `minmax(0,1fr)`）が残っていることを機械で見る検査を足した。
**この検査は、実際にルールを外して落ちることを確かめてある。**

---

## D-037. 毛（coat）── 頭ではなく「輪郭」に生やす

製品オーナーから「生き物の形質として、毛の要素を加えたい」という要望。
案を 8 つ出して選んでもらい、**体の輪郭に沿って外向きに生える体毛**を採る
ことにした（他の案は、髪・まゆ毛・ひげ・たてがみ・耳の内側の毛・毛の長さの
数値形質・毛色の分離）。

### なぜ「輪郭に沿わせる」を最初に選んだか

1. **頭頂はもう空いていない。** 植物・角・触角・結晶がすべて頭頂から生えて
   いて、D-033 で「別々の装飾の根元が横方向に離れているか」の自動検査まで
   入れた場所。髪をそこへ足せば、その検査に必ず掛かる。輪郭の全周へ散らす
   方式なら「1 か所から生えている器官」ではないので `roots` を宣言する必要が
   なく、既存の装飾と場所を奪い合わない。
2. **全個体に効く。** 輪郭は素体・シルエットが何であっても必ずあるので、
   3 素体 × 6 輪郭のどれにも同じ規則で乗る。
3. **顔を覆わない。** 毛は外向きにしか伸びないので、`face-covered` の
   検査に触れる余地がそもそもない。

### なぜ `texture`（質感）に混ぜなかったか

`texture` は排他なので、毛をそこへ足すと「ゼリー質のもふもふ」「しんじゅ色の
つんつん」が作れなくなる。独立したロカスにして掛け算にするほうが、交配で
観察する対象としての情報量が多い。

### 描き方 ── 実績のある「背面パーツ」の作法をそのまま使う

毛は体の内側から始めて外へ伸ばし、体内部分を `bmaskout` で切り落とす。
切り口はちょうど輪郭と重なり、その上から `Z.OUTLINE` が輪郭を引き直すので、
付け根は太いインク線の下に隠れる ── 足・耳・尾・羽とまったく同じ手。
`Z.COAT = 10` は羽（12）よりさらに奥で、器官の付け根が毛に埋もれない。

うすぎぬ（veil）用の「体内が透けるレイヤ」だけは毛に付けていない。毛が体内に
持っているのは 3px ほどの細い根だけで、透かしても絵にならず、パスの数
（1 個体あたり最大 96 本）が倍になるだけだから。

### 2 度、作り直した記録

**(1) 長さと太さだけでは 5 種を描き分けられなかった。**
最初の版は「間隔・長さ・太さ・反り」の 4 つで うぶ毛／もこもこ／つんつん／
ぼさぼさ／ひとすじ を作った。ところが実際に並べると、**もこもこ・つんつん・
ぼさぼさが「同じとげの長短」にしか見えない**。ふわふわととげとげを分けて
いるのは長さではなく **輪郭の曲がり方と先端の形** だったので、
- `taper`（側面のふくらみ。0.5 でまっすぐな三角、大きいほど葉、小さいほど針）
- `blunt`（先端の丸み。0 でとがり、大きいほど丸い房）

の 2 つを足した。これで初めて「丸い房」と「まっすぐなとげ」が別物になった。

**(2) もこもこが「レース編みの縁飾り」になった。**
房を重ねれば塊に見えるはずだったが、毛は 1 本ずつ独立した閉じたパスなので、
線を引くと **重なった相手の輪郭も全部見える**。結果、輪が鎖状に並んだ
クロシェ編みの縁になった。`ears.ts` の `organShapeOpen` と同じ
「インクで塗りつぶした版 → 本体色で塗り直す版」の 2 枚重ねに変えて、
union の外周にだけ線が残るようにした。逆に 1 本 1 本を見せたい毛
（ぼさぼさ・ひとすじ）は交差した線がそのまま「別の毛」に読めるので、
1 枚描きのままにしてある（`CoatStyle.merge`）。

### viewBox と地面 ── 3 回に分けて塞いだ穴

毛は輪郭の **外** へ出る初めてのパーツなので、既存のどのテストも
「どこまで伸びたか」を見ていなかった。実際に出た不良は 3 つとも別の原因:

| 何が出たか | 原因 |
|---|---|
| `out-of-view:coat(0.0,33.1,201.9,175.7)` | bbox を「付け根の中心 ＋ 余白 wid」で作っていた。先端側にも同じ余白が付き、何も描かれていない外側まで bbox が伸びた |
| 先端の丸みぶんのはみ出し | 長さの上限は先端 1 点しか見ていなかったが、`blunt` の弧と線幅はその外へ張り出す |
| 接地素体で毛が地面と影を突き抜ける | 長さは抑えたが、付け根の左右へ `wid` 広がる「肩」は長さと無関係。接地線の下端でここが地面を割っていた |

`tests/coat.test.ts` は 3 つとも回帰として固定してある（5 種 × 60 seed ×
成体/幼体で viewBox と接地線を検査）。1 つめと 3 つめは、このテストを
先に書いたことで見つかった。

### 旧セーブの取りこぼしを 1 つ塞いだ

遺伝子座を新設すると、それ以前に保存された個体の `cat` にはその座が無い。
表現型側（`catPairOrDefault`）はカタログ先頭で補っていたが、
**`genetics/breeding.ts` は素で `parentA.cat[locus][0]` を読んでいた** ──
つまり旧セーブの個体は交配しようとした瞬間に落ちる。`coat` を足したことで
初めて現実の危険になったので、`pairOrDefault` / `numPairOrDefault` を足して
表現型側と同じ規則に揃えた。カタログ先頭は各ロカスとも既定形質（多くは
'none'）なので、旧個体に新形質が勝手に生えることはない。

### 発現率

`lashes` と同じく **dominance を全て同値（3）** にしてある。同値ヘテロは
seed 由来の 50/50 抽選になるので、`weight` の比がそのまま
「その毛を持つ個体の割合」として読める。none 以外の合計 36%。

### 検査

- 自動検査のベースラインは **変更前と完全に同一**（成体 300 体で
  `decor-same-root:plant/horns` 1 件のみ。これは既知の false positive）。
- 600 個体 × 5 種で「毛なし → 毛あり」の差分を取り、**新規に増えた不良は 0 件**。


---

## D-038. もこもこは「縁の飾り」ではなく輪郭そのもの／ひとすじの取り下げ

D-037 で入れた毛を製品オーナーが実物で確認した結果、2 つの指示が出た。

> 「ひとすじ」キモすぎるから完全に削除して。
> もこもこは、既存の体の輪郭が際立ちすぎて変だから、（参考画像のように）
> 元の体の輪郭を消す感じがいいかもしれない。

### ひとすじ（wisp）の取り下げ

AGENTS.md §2 の手順どおり:

1. `CAT_LOCI` の `coat` から `wisp` を削除（新規個体には二度と出ない）
2. `render/parts/coat.ts` の `wisp` スタイルは **残す**。見出しに
   `！！このコードは消さないこと！！` を付けた。既存セーブが遺伝子型に
   `wisp` を持っている可能性があり、消すとその個体の毛だけが無くなる
3. `weight` を 0 にするような中途半端はしない

`ocelli`・`button`・`mossRing`・`shard` と同じ扱い。

### もこもこ ── 何が「変」だったのか

房は体の **外側** に描き、体内側はマスクで切って、その上から
`Z.OUTLINE` が体の輪郭を引き直していた（D-037 の設計）。
その結果 **房の内側になめらかな輪郭が 1 本残る**。人はその内側の線を
本体の形として読み、外の房を「縁に付けた飾り」と解釈する。
実測（`B4KQ-YHUV` `HG8G-W7HU` `FDLZ-T9CU`）ではどれも
**縁をピンキングばさみで切った紙**に見えていた。

直しかたは「房が輪郭そのものを担う」。`CoatStyle` に `owns` を足し、
もこもこだけ true にした。true のとき:

- `body.ts` は体の輪郭を引かない（`Z.OUTLINE` のパーツを作らず、
  本体の塗りからも stroke を外す）
- 房のインクを `strokeThin` から **`strokeW`**（＝体の輪郭と同じ太さ）へ。
  細いままだと「本体より頼りない線で囲まれた形」になり輪郭として読めない

### 見落としていた「2 本目の輪郭」は 1 つではなかった

輪郭パーツを消しただけでは足りなかった。体の輪郭に沿って線を引いている
場所が、ほかに **9 か所** あった。

| 出どころ | 何を引いていたか |
|---|---|
| `buildBaseAccent`（まる型） | 輪郭沿いのやわらかい陰 5px |
| `buildBaseAccent`（すみ） | 無彩色の締め 6.5px / 2.6px |
| `buildTexture`（すりガラス） | 白い帯 12px / 8px |
| `buildTexture`（こうぶつ） | 明るい線 1.4px |
| `buildTexture`（すきとおり） | 屈折ハイライト strokeW×2.2 |
| `buildTexture`（うすぎぬ） | 縁の重なり strokeW×3.4 / ×1.6 |
| `buildTexture`（マット） | 輪郭内側の陰 7px |
| `lumin.ts`（ふちひかり） | 内側の縁光 strokeW×1.2 を不透明度 0.62 |

最初は「細い線だけ引かない／太い帯は残す」で試したが、**残した帯が
そのまま輪郭を描き直していた**（すりガラスの白い帯が房の内側に
なめらかな縁として出た）。太さに関わらず輪郭沿いの効果は全部やめる、に変えた。
`rimPath()` という 1 つの関数に集約してあるので、今後この種の効果を足す人は
そこを通せばよい。

`lumin` の `rim`（ふちひかり）だけは別扱い。外側のにじみ（`luminBack`）は
房の位置まで広がるので残し、**内側の縁光（`luminRim`）だけ置かない**。
「縁が光っている」という読みは外側のにじみだけで足りる。

### 教訓 ── 「輪郭を消す」は 1 か所では終わらない

輪郭は `path(shape.d, {stroke})` という形で **絵のあちこちに散っていた**。
目で 1 個体だけ見て「消えた」と判断していたら、質感が違う個体で必ず戻る。
そこで **機械的な検査**を書いた（`tests/coat.test.ts`）:

> もこもこの個体について、体と同じ `d` を持ち・ぼかしが掛かっておらず・
> 線幅 8 未満・不透明度 0.3 以上の `<path>` が 1 つでもあれば落とす。

260 個体で 0 件。**この検査は、`owns` を false に戻して実際に落ちることを
確かめてある**（D-033 の教訓：落ちない検査は無いよりたちが悪い）。

### 確認したこと

- 自動検査の不良率はベースラインと **完全に同一**
  （全体 1/600・もこもこ強制 1/400。`owns` の on/off で数字が動かない）
- 質感の見分けは、縁の帯ではなく **面の中身**（粒・霞・多角形・点光沢）が
  担っているので、縁を落としても質感は読める
- ユニット 255 件・ビルド・e2e はすべて通る

### 残っている小さな問題

模様（pattern）は体のシルエットで clip されるので、**模様の縁が
元のなめらかな輪郭の形で切れる**。インクの線ではないので目立たないが、
模様の濃い個体では「毛の内側に別の形がある」と読めなくはない。
毛は皮膚の外にあるものなので理屈は合っており、今回は直していない。


---

## D-039. 目の色の溜まり・きらめき・閉じ目のまつげ・耳の中身（2026-08-22）

製品オーナーから 5 件の指摘が同時に来た。

> このタイプの目、真ん中の楕円のちょっと色濃い部分、もう少し色濃くしてほしい。
> あと、この楕円の部分が真円の個体もいるようにしてほしい。
> したりめとまつ毛が、全然マッチしていない。しっかり確認して直してほしい。
> あと、目について、稀に（参考画像の）ようにキラキラがある個体がいるようにしてほしい。
> 耳も今、外周と一致しているのはいいけど、中身が変だよね。

添えられた画像は 2 枚。1 枚目は紫の体に teal の丸い目（`HG8G-W7HU` を
`pupil=round` で描いたものと一致した）。2 枚目は紫の目に丸いつやが 2 つと
4 方向の星のきらめきが 1 つ。

### 1. 「真ん中の楕円」の正体を先に特定した

指摘の「楕円のちょっと色濃い部分」は、意匠『まるい』（`pupil=round`）が
虹彩の中央へ置いている面
（`motifMarkup` の `ellipse(0, py+s*0.08, s*0.48, s*0.32, deep, 0.14)`）。
点目（`drawSolidEye`）にも似た楕円があるので、`sheet.html?force=` で
両方を描き分けて画像を突き合わせ、**『まるい』のほうだ**と確定してから
直した。ここを取り違えると「濃くしたのに変わらない」になる。

- 濃さ … 不透明度 0.14 → **0.32**。色は `deep`（面の色を落としたもの）の
  まま。ここへインクや黒を置くと、廃止したはずの瞳孔が復活して
  「見つめてくる目」に戻る（D-018 の方針）。
- 形 … 個体ごとに **横長の楕円（0.48×0.32）／真円（0.40×0.40）** を
  切り替える。`ctx.rng('eyeMotif')` の名前付きサブストリームを使うので
  決定論的で、しかも左右の目は同じ系列を引くため必ず同じ形になる
  （目が「対」に見えることの担保）。面積をほぼ揃えてあるので、
  どちらでも濃さの印象は変わらない。

### 2. きらめきは `pupil` に足さず、新しい遺伝子座にした

`pupil` は「目の中の意匠」を 1 個体 1 種類だけ持つ座。ここへ足すと
**きらめきが出た個体はうずまきもはなびらも持てない**ことになり、
「どの意匠にも乗る飾り」にできない。まつげ（`lashes`）を目の形と
分けたのと同じ理由で、独立した座 `eyeGlint`（なし / きらり）を新設した。

- D-037 の規則どおり、カタログ **先頭は `none`**。既存セーブにはこの座が
  無く、読み込み側（`catPairOrDefault` / `pairOrDefault`）は先頭の対立
  遺伝子をホモで補うので、先頭を `star` にすると飼育中の全個体に一斉に
  きらめきが生える。実際に「座を消したセーブ」を作って `none` で補われる
  ことと、その個体どうしの子も `none` になることを確認した。
- dominance を両方同じにして「発現率＝対立遺伝子頻度」にした
  （`lashes` と同じ設計）。weight 5 ＝ **20 体に 1 体**。
- 絵は既定のハイライト（丸・左上・全個体に 1 つ）とは別物として、
  **4 方向の星を大小 2 つ**、面の右下へ置く。丸を増やすと「全個体が同じ
  キラキラ目」になるという既定ハイライトの約束を壊さずに済む。
  尖った星（`starPath`）ではなく `roundedStarPath` を使う ── 小さいので
  尖った星は 96px でただの三角形に潰れる（『ほしぞら』で実測済み）。
- 白目のある目・べた目（たまご）・点目（つぶら）・閉じ目のすべてに乗る。
  面のある目では `clipPath` の内側に描くので、面からはみ出すことがない。
- `DEFAULT_HEAVY`（`tests/genetics.test.ts`）へ登録した。「稀に」が要件の
  座なので、既定値が支配的なのは設計どおり。

### 3. したりめ × まつげ ── ずれの正体は「縮小の中心」だった

実物を測って原因を特定した（`B4KQ-YHUV`・したりめ・ロング）:

| | 幅 |
|---|---|
| 目の弧（`eye0` の bbox） | 44.3px |
| まつげ（`lash0` の bbox） | 30.2px |

まつげが弧より **14px も短く**、しかも弧の中央に置かれていたので、
目尻側に 5px・目頭側に 9px、弧の細い線が両側からはみ出していた。

短くなる理由は縦横比。閉じ目の器は `rx:ry = 1.12:0.52` と平たいのに、
原画のまつげは幅 1 に対して高さ 0.41 ある。高さの上限に必ず当たるので、
まつげは弧の 6〜7 割の幅までしか伸ばせない。

**問題は「短いこと」ではなく「中央で縮めていたこと」。** 原画は
「目頭側が細く尖り、目尻側が太くて枝分かれ（プロング）がある」構図なので、
中央で縮めると **太くて枝分かれのある側が目の真ん中に来て、その外へ細い線が
生える**。まつげにも目にも見えない形になる。

直しかた（`drawLashes`）:

- 閉じ目では目尻を `rx` ではなく **弧の端 `rx * 1.08`** とする
  （`drawEye` が `const w = rx * 1.08` で弧を引いているのに、まつげ側は
  `rx` を目尻だと思っていた）。
- 縮めるときに **目尻側を不動点** にする（`pinOuter`）。すると
  枝分かれは目尻の **外**（まつげが生える場所）へ出て、原画の細く尖った
  目頭側が弧の細い線へそのまま続く。弧とまつげが 1 本の線として読める。
- 下まつげ用の高さ上限（`place.maxH`。白目の中へ帯を入れないための値）は
  閉じ目では使わない。白目が無く上下とも同じ弧が基準なので、あの上限を
  当てると下まつげだけが弧の 2 割の幅に縮んで「弧の端に付いたゴミ」になる。
  閉じ目は一律 `1.15ry`。
- 開いた目の絵は **1px も変わらない**。`pinOuter` でないときの
  `startXOf` / `anchorXOf` は、従来の `spanC` 基準の式と恒等的に同じ値を
  返すように書いてある。

### 3.5. 弧の内側の線も直した（同じ指摘の一部）

閉じ目には「閉じた目であること」を伝えるための虹彩色の細い線がある。
これが `M-0.7w 0.42h Q0 -0.52h 0.7w 0.42h` という **ふくらみの違う別の弧**
で、頂点はインクの弧より 0.375h も下にあった。人はこれを
「まぶたの内側の色」ではなく **2 本目のまぶた** と読む。そこへまつげが
乗ると線が 3 本並び、何の絵か分からなくなる。

インクの弧と **同じ曲線を線の太さぶん真下へずらしただけ**の線にした。
2 次ベジエは区間を切り出しても 2 次ベジエのままなので、両端を少し詰めた
同じ形を de Casteljau で求めて定数に焼いてある（`closedInnerArc`）。

### 3.6. 切り欠きの mask はまつげに掛けない

したりめの identity である切り欠き（弧の右寄りの小さなくり抜き）を、
まつげにも同じ mask で掛けてみた。ところがまつげは弧より厚いので、
切り欠きの円が **まつげの中に開いた白い丸** になった。まつげに乗る
白い点は「目の上を魚が泳いでいる」と読まれて一度外した経緯があるので
（`drawLashes` の白いつやの説明）、掛けないことにした。
切り欠きは弧の右寄りに固定・まつげは目尻側に寄るので、重なるのは左右
どちらか一方の目だけ。もう一方では切り欠きがそのまま見え、したりめの
非対称さはむしろ強まる。

### 4. 耳の「中身」── 規則を 1 つにする

内耳の面・耳先の色・葉脈は、それぞれ `center` と `reach`／`lift` から
**独立に置いた図形**だった。外周は `upper`／`lower`／`apex` を通る曲線
なのに、中身は曲線を一切見ていない。結果、形ごとにばらばらに壊れていた:

| 耳 | 壊れかた |
|---|---|
| まるみみ | 面が外周の左上へ丸ごとはみ出し、背景の上に乗る |
| うさ耳 | 面が耳の底で外周を割り、体の上へ出る |
| ねこ耳 | 硬い三角形が外へ突き出す（この作品は尖った三角を使わない） |
| たれみみ | 面が耳ではなく **頭の上** に乗る |
| このはみみ | 葉脈が根元を越えて体の上まで落ちる |

個別に座標を調整せず、**規則を 1 つにした**（`integratedEar`）:

1. 中身はすべて耳の塗り（`fillPath`）で **クリップする**。
   「外周からはみ出す」ことが構造的に起こらなくなる。
2. 内耳の面は **耳の外周そのものを相似縮小した形**にする。
   どんな耳でも中身が外周と同じ曲がりかたをする
   ＝ 製品オーナーが評価した「外周と一致している」を中身にも広げる。
3. 耳先の色は **先端を不動点** にして縮める。どの形でも「先端だけ色が違う」
   になる。

縮小の中心は `center`（根元の中点）と `apex`（先端）の中間 **ではない**。
最初そうしたら、まるみみで内耳が「頭寄りの三日月」になった。耳が外へ
ふくらむのを作っているのは通過点ではなく **制御点** なので、制御点を数に
入れない中心は実際の面の中心よりかなり内側へ来る。`open` に書かれている
座標（通過点＋制御点）の平均を使う。ベジエ曲線は制御多角形の凸包に必ず
収まるので、その平均は形がどう変わっても面の中に入る。

### テストを 1 つ緩めた（代理ではなく規則を見る）

`tests/render.test.ts` の「一体化した耳は変形行列で線幅を変えず…」は
`scale(` が 1 つでもあれば不合格、という書きかただった。守りたい規則は
**耳の輪郭線の太さを変形で変えない**ことで、`scale(` の有無はその代理。
内耳が「外周を相似縮小した **塗り**」になった今、線幅は 1px も変わらない
のに代理の検査だけが落ちる。規則そのもの ＝ 「縮小したグループの中に
`stroke=` を入れない」を見るように書き換えた（`scaledStroke`）。

### 確認したこと

- ユニット 255 件・`tsc --noEmit`・`vite build`・e2e 47 件すべて green
- 自動検査の不良率はベースラインと **完全に同一**
  （成体 300 体で 1 件・内訳も `decor-same-root:plant/horns:1` のまま）
- `dist/` は `index.html` と assets だけ・`holo-card` の文字列は 0 件
- 既存セーブ互換: `eyeGlint` を持たない遺伝子型で表現型を作ると `none`、
  その 2 体を交配した子も `none`
- 実物の目視: したりめ／みかづき × まつげ 9 種 × 3 個体、耳 8 種、
  意匠『まるい』12 個体、きらめきを目の形 5 種に強制したもの、
  96px の一覧 60 体（問題個体 0）

### 手を付けなかったこと

たれみみ（`flopEar`）の **外周そのもの** が細いかぎ形で、中身を入れても
薄い三日月にしかならない。これは中身ではなく silhouette の問題で、
今回の指摘（「外周と一致しているのはいい」）の範囲外なので触っていない。

---

## D-040. きらめきを強める／虹彩の二色グラデ（D-039 の続き・2026-08-22）

D-039 のきらめきを実物で見た製品オーナーから追加の指示。

> キラキラの目、もっとキラキラでもいいよ。
> あと、稀に目の中が二色の美しいグラデかかっている個体もいてほしい

### きらめきを強める ── 「白い図形」から「光」へ

星を 2 つ → **3 つ**（主星 ＋ 右下 0.52 倍 ＋ 左上 0.36 倍）にし、
主星の不透明度を 1.0 にした。大きさを変えて散らすのが要点で、
**同じ大きさで並べると「点線」に見えてきらめきにならない**。

決め手は星の数ではなく、裏に敷いた **光のにじみ** だった。
最初は不透明度 0.16 の白い円を置いたが、縁がはっきり出るので
光ではなく **白い水玉** に見えた。中心 0.5 → 外周 0 の放射
グラデーション（`glintHalo`）に置き換えて初めて「光っている」になる。
中心でも 0.5 止まりなのは、濃くすると面の色（＝その子の配色）が
白く飛ぶため。にじみは面の `clipPath` の内側で描くので外へは漏れない。

### 二色グラデ ── `eyeGlint` とは別の座にした

`irisTone`（ふつう / ふたいろ、weight 6 ＝ 稀）を新設。`eyeGlint` に
相乗りさせなかったのは:

- あちらは面の **上に載る白い飾り**、こちらは **面そのものの塗りかた**。
  同じ座にすると「きらめきとふたいろを両方持つ個体」が作れない
- どちらも「稀」なのに、同じ座だと互いの確率を食い合う

D-037 の規則どおり **カタログ先頭は `none`**。両方の座を消した遺伝子型で
表現型を作ると `none`、その 2 体の子も `none` になることを実際に確認した。

**相方の色は配色ファミリーから借りない。** `accent` や `petal` を使うと
装飾（角・花・首かざり）と同じ色が目に入り、グラデではなく
**模様が映り込んでいる** ように読める。虹彩そのものの色相を
±46°／±74° 回して作れば、どの配色でも必ず隣り合う 2 色になり濁らない
（ずらし量は `ctx.rng('irisTone')` の名前付き乱数。左右の目は同じ系列を
引くので同じ向きにずれる）。

**放射ではなく線形にする。** 既定の虹彩グラデは「球の照り」を作るための
放射で、中心から同心に色が変わる。ここへ 2 色目を入れると
**輪の模様**（＝意匠『わっか』）に見えてグラデーションとして読めない。
上から下へ流れる線形にすると混色の帯が 1 本だけできて
「上が〇色・下が〇色」と読める。球に見せるぶんはハイライトの丸が担う。

明度の上限は 66。べた目（たまご）では虹彩がそのまま体と隣り合うので、
相方だけ明るいと目の上半分が白く飛んで「白目が戻った」ように見える
（`tallEyeFill` が本体側を 30〜60 に収めているのと対になる値）。

### 確認したこと

- ユニット 255 件・`tsc --noEmit`・`vite build`・e2e 47 件すべて green
- 自動検査の不良率はベースラインと **完全に同一**
  （成体 300 体で 1 件・内訳も `decor-same-root:plant/horns:1` のまま）
- 実物の目視: 白目あり／べた目（たまご）／点目（つぶら）／ぱっちり×わっか、
  きらめきとの併発、96px の一覧 20 体ずつ（どちらも問題個体 0・
  96px でも二色が読める）
- 既存セーブ互換: `eyeGlint`・`irisTone` の両方を持たない遺伝子型でも
  親・子とも `none`

---
---

## D-041. 鑑定 ── 「見えているもの」と「持っているもの」を分ける

製品オーナーの要望は「通常プレイでは遺伝情報を全部見せない／鑑定して初めて
内部が分かる」。Genotype は最初から決まっているので、**鑑定は生成ではなく
開示** として実装する（`game/grading.ts`）。

    序盤: 見た目から推測して掛け合わせる
    後半: 遺伝型を理解して系統を設計する

この 2 段階を作ることが目的で、情報を隠すこと自体は手段でしかない。

### 伏せる側は 1 画面では終わらない

詳細画面だけ伏せても意味がない、というのが実装で最も時間を使った点。
実ブラウザで通しで確認して、**確定情報が漏れる経路が 4 つ**見つかった。

| 経路 | 何が漏れていたか |
|---|---|
| 展示会の選択カード | 未鑑定個体の rarity tier ピル。詳細画面が「まだ分からない」と言っているすぐ隣で「ふつう」と断定していた |
| 展示会の結果「希少性」 | `judgeScale(rarity.score/100)` の単調変換を 0..100 の整数で表示。**表示から rarity.score をほぼ復元できた** |
| 審査員のコメント | `rarity.reasons[0]` をそのまま読み上げていた（鑑定でしか分からないはずの内訳） |
| 育成室の成体化 | `rarity.tier` が rare/precious のときだけ きらめき音。音が鑑定より先に答えを言っていた |

直しかたの方針:

- **数字は伏せ、印象は残す。** 希少性のバーは残して数字だけ「？」にした。
  バーの長さからも読めるので、未鑑定では 10 点刻みへ丸めている。
- **総合点は伏せなくてよい。** 5 項目の重み付き平均に ±6% のゆらぎが乗るので、
  総合点と他 4 項目から希少性を解いても ±20 点以上ぶれる。逆算は成立しない。
- **音も情報である。** 鳴らす条件を `observedRarity`（見た目に出ている珍しさ）に
  替えた。プレイヤーが自分の目で気づけることを音が後押しするだけなら、
  鑑定前に渡してよい範囲に収まる。

市場価格は伏せていない。基本価格と「市場評価（内訳未鑑定）」の 2 行にまとめ、
希少度の寄与だけを引き算できないようにした。価格を完全に隠すと
「売れる／売れない」の手がかりまで無くなるので、粗いヒントとしては残す。

### レポートは二重実装になっている（承知のうえ）

発現の正本は `genetics/phenotype.ts` の `expressCat` だが、公開されていないので
`grading.ts` に同じ規則を写している。**片方だけ直すとレポートが嘘をつく**
（画面には「発現：A」と出るのに、絵は B で描かれる）。型もテストも通ってしまう
種類の事故なので、`tests/grading.test.ts` に
「全カテゴリ座 × 120 個体で、レポートの発現・保因が実際の Phenotype と一致する」
という突き合わせを置いた。優劣の判定を反転させて実際に落ちることを確認済み。

この検査を書いた時点で、**本物の食い違いが 1 つ見つかった**。
耳が無い個体は耳先色が描かれない（`earTip: expr.ears.id === 'none' ? 'none' : …`）。
同じ差し替えは羽（スライム型では出ない）と足（まる型では `none` のとき
**隠れていた側**が出る）にもある。「発現：みみさき色」とだけ出すと
すぐ上の「見えている特徴：耳先色 なし」と矛盾して読めるので、
姿と食い違うときは理由を添えるようにした（`suppressed`）。
育種家にとっては「持っているのに出ていない」こと自体が情報なので、伏せない。

### 鑑定料 160 コイン

現行の経済で見ると:

    展示会 1 回（B ランク）… 30 + 総合点×1.6 + 35 ≒ 160〜170 コイン
    交配 1 回 … 80 コイン
    成体の売却 … 600 コイン以上

**鑑定 1 回 ＝ 展示会 1 回** に相当する。初回展示会は下限 120 コインなので、
1 回目では届かず 2 回目で払える。序盤は「育てて → 展示会に出て → 鑑定する」
という順番が自然に生まれ、後半は売却収入の前で相対的に無料になる。
情報の価値は経済ではなく体験の側にあるので、後半で安くなること自体は問題ない。
**この価格のまま据え置く**ことを提案する。

### 開示の瞬間を作る

トースト 1 行で終わっていたので、押した瞬間には何も起きず、
下へスクロールして初めて表が増えていることに気づく状態だった。
払ったものが何に化けたのかを、その場で 1 枚のダイアログに出す
（tier・正確な点数・かくれて持っていた めずらしい形質の名前・希少度の内訳）。
紙吹雪は rare / precious のときだけ。毎回だと「特別」が薄まる。

### レポートは「資料」ではなく「読みもの」にする

26 + 24 座をカタログ順に並べると、`結晶 HOMO なし/なし` のような
何も起きていない行と、`耳先色 HET なし/みみさき色` のような次の世代に効く行が
同じ重さで並ぶ。先頭に見どころを数行だけ出し、全座は注目順
（珍しい保因 → 姿に出ていない → 共優性 → ふつうの保因 → ホモ）に並べ替えた。
HOMO / HET は初見のプレイヤーに通じないので、日本語で 1 行の説明を置いた。

### フレーバーは鑑定の報酬にしない

これは遺伝情報ではなく、その個体の外側に世界が続いていることを見せる観察記録。
鑑定の後ろに置くと、いちばん character の出る一文が
「お金を払うまで読めないもの」になり、育てている間の画面が乾く。
成体なら鑑定の前後を問わず「観察ノート」として出す。

文の数は実測で決めた。最初の版は **600 個体で 39 種類・最頻の 1 文が 11%・
半数の個体が 11 種類に集中**していた。原因は条件別の候補が 1〜2 本しか
無かったこと（「毛がある」は個体の 36% に当たるのに候補が 1 本だった）。
条件を増やすより各条件の候補を厚くするほうが効き、
**125 種類・最頻 3.5%・半数が 29 種類**になった。
検査も「種類が 8 より多い」から偏りを見る形へ書き直した。

### appraisedAt は まだ Creature の正式フィールドではない

`Creature` に無い追加キーとして載せている。通常の save → JSON → load では
往復するが、**壊れたセーブの救出（`coerceCreature`）は 1 フィールドずつ
組み直す**ので、拾わなければ落ちていた。プレイヤーから見れば
「壊れたセーブを直したら、お金を払った鑑定だけ無かったことになった」。
そこだけ先に塞いだ（`save/schema.ts`）。

型の正式化は save / validateState / coerceState / migrate / createCreature /
released record / tests まで一貫して直す作業なので、**中途半端に型だけ足さない**。
いまは「データを落とさないこと」だけを保証している状態。

### 番号について

D-039 / D-040 は、同じ日に並行して進んでいた別の作業（目・耳）が使っている。
そちらを先に master へ入れたので、この記録はその後ろに並ぶ。


（以降の判断は実装の進行に応じて追記する）
