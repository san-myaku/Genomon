# TEST_REPORT.md — テスト結果（実測値のみ）

最終実行日: 2026-08-04

すべて実際にコマンドを実行した出力に基づく。未実行の項目は「未実施」と明記する。

## 総合

```
npx tsc --noEmit   → エラー 0
npx vitest run     → Test Files 12 passed (12) / Tests 192 passed (192)
npx playwright test→ 20 passed（PC 10 / スマホ 10）
npx vite build     → ✓ built
                     dist/index.html                0.50 kB │ gzip:  0.37 kB
                     dist/assets/index-*.css       39.39 kB │ gzip:  8.53 kB
                     dist/assets/index-*.js       280.28 kB │ gzip: 97.95 kB
```

外部ランタイム依存はゼロ（devDependencies のみ）。

### 2026-08-22 の実行（Cards Lab 追加後）

上の「総合」は 2026-08-04 時点の実測値で、その後の反復を反映していない。
Cards Lab を足した時点で全部を回し直した結果を、上書きせずに併記する。

```
npx tsc --noEmit    → エラー 0
npx vitest run      → Test Files 18 passed (18) / Tests 250 passed (250)
npx playwright test → 46 passed / 7 skipped / 1 failed（skip は幅を限定した検証。
                      failed は `05-field` の 1 件で、フルスイートでのみ落ちる
                      既知の flaky。徘徊する別個体とトーストがクリック対象に
                      重なるのが原因で、単独実行では通る（実際に
                      `--project=pc` 単独で 1 passed を確認）。落ちる側は実行
                      ごとに pc / mobile を行き来する）
npx vite build      → ✓ built（78 modules）
                      dist/index.html               0.52 kB │ gzip:  0.38 kB
                      dist/assets/index-*.css      65.99 kB │ gzip: 13.40 kB
                      dist/assets/index-*.js      399.71 kB │ gzip:137.84 kB
```

`dist/` の中身は `index.html` とその assets だけ。`lab.html` / `sheet.html` は
出ておらず、`grep -l "holo-card" dist/assets/*` も 0 件（Cards Lab と
`@kongyo2/cards-css` が配布物へ漏れていないことの確認）。

新規追加ぶん:

| ファイル | 件数 | 対象 |
|---|---|---|
| `tests/coat.test.ts` | 13 | 毛の描画（D-037/D-038）。全種類が描かれる・種類ごとに違う絵になる・決定論・体マスク・viewBox と地面をはみ出さない／**ひとすじが新規個体に出ないこと**と**既存セーブの wisp が今も描けること**／**もこもこに outline パーツが無いこと**・**体と同じ形を線として描き直しているパーツが 1 つも無いこと**（260 個体）・不良が増えないこと |
| `tests/cards.test.ts` | 12 | カード事実の決定論（2 回導出して完全一致）・foil テクスチャ seed の個体差・Grade を変えても他が動かないこと・証明書番号と通し番号の体裁・実データを使っていること（模様/配色/希少度）・Finish がライブラリに実在する effect を指すこと／**CARD_CSS に制御文字が混ざっていないこと・波かっこの対応・スマホ用の要（`--cards-deck-h`、`.cards-right` の `minmax(0,1fr)`）が残っていること** |
| `e2e/06-cards-lab.spec.ts` | 13×2 | `?tab=cards` で直接開ける・Showcase/3 種の比較がすべて出る・**Finish 比較の 6 枚が別の箔で、静止状態でも箔が見えている**・Design 切り替えでカードが差し替わる・カード内に実際の `gm-svg` が入っている・**320/360/393/768px のどの幅でも要素が画面をはみ出さない**（`scrollWidth` ではなく矩形で見る）・**スマホ幅でタップ対象が 38px を下回らない**・**Visual Lab の設定キーを書き換えない**／スマホ限定: **カードがデッキバーに隠れず 1 枚まるごと見える**・「つぎのカード」で 4 回めくって 4 体とも別個体・「戻る」で履歴が戻る・デッキの「版面」「箔」が 1 タップで変わる・左右スワイプでめくれる・**320/360/393px のどれでもデッキの主役が 1 行に収まり、カードがデッキに隠れない**／PC 限定: デッキバーが出ず、比較が畳まれていない |

E2E の主要な検査は、実際に一度壊した箇所の回帰テスト。
「静止状態でも箔が見えている」は、比較の 6 枚が 6 枚とも同じ絵に見えていた件
（VISUAL_QA の C-4）。「カードがデッキバーに隠れない」は、スマホでカードの
下端が 29px 潜っていた件（C-10）。どちらも目分量では気づけず、
要素の矩形を測って初めて数字が出た。
「要素が画面をはみ出さない」は、`.cards-right` の暗黙カラムが max-content で
380px に固定され、320px の画面で右が切れていた件（C-16）。
**`body{overflow-x:hidden}` のせいで `scrollWidth` を見る旧テストは通っていた。**
`tests/cards.test.ts` の CSS 検査は、実際にルールを外して落ちることを確認済み。

## テストファイル別

件数は `vitest --reporter=json` の出力から集計した実測値。

| ファイル | 件数 | 対象 |
|---|---|---|
| `tests/save.test.ts` | 69 | 保存/読込・破損復旧・バックアップ・チェックサム・ショップ整合性 |
| `tests/migrate.test.ts` | 32 | v1→…→v7 連鎖マイグレーション |
| `tests/genetics.test.ts` | 25 | seed再現性・表現型変換・500体生成・支配的形質の検査（`eyeGlint`／`irisTone` は「稀に」が要件なので `DEFAULT_HEAVY` 側） |
| `tests/breeding.test.ts` | 17 | 交配・優性潜性・突然変異・親子/兄弟類似性・多世代多様性・希少度分布 |
| `tests/game.test.ts` | 13 | 世話・展示会・ショップ・クールダウン永続化 |
| `tests/cards.test.ts` | 12 | カードの決定論・CSS の制御文字と波かっこの対応 |
| `tests/field.test.ts` | 11 | 飼育フィールドの生活状態・排泄物・配置 |
| `tests/render.test.ts` | 11 | 1000 体で描画不良・非決定的 SVG を出さないこと／耳は **縮小したグループの中に `stroke=` を入れない**（`scaledStroke`。内耳の面は外周の相似縮小なので `scale(` 自体は出る。D-039） |
| `tests/inspect.test.ts` | 9 | **描画の自動検査そのものの検査**（見逃しと誤検出の両方を固定） |
| `tests/lashes.test.ts` | 9 | まつげと目の位置関係・体からのはみ出しを増やさないこと |
| `tests/coat.test.ts` | 8 | 体毛が viewBox と接地線を越えないこと・種類の描き分け・不良を増やさないこと |
| `tests/progression.test.ts` | 8 | 通しプレイ・進行不能検査・オフライン・間欠プレイ |
| `tests/icons.test.ts` | 6 | UI アイコン名の実在・同一画面内の重複・自己完結した SVG であること |
| `tests/integration.test.ts` | 6 | 複数世代の交配・どの段階でも次の案内が出ること |
| `tests/nonvisual.test.ts` | 4 | 見た目に依らない状態遷移 |
| `tests/satiation.test.ts` | 4 | 連打が有利にならないこと（1 秒あたり効率・必要クリック数） |
| `tests/contrast.test.ts` | 3 | 牛柄とインクの 3.0:1 を**明暗両テーマ**で固定 |
| `tests/staff.test.ts` | 3 | 飼育員の募集・雇用・報酬 |
| **合計** | **250** | 18 ファイル |

## 遺伝の統計検証（実測値）

| 項目 | 要求 | 実測 |
|---|---|---|
| 親子の類似度 vs 無関係な2個体 | 差 > 0.12 | 親子 **0.5907** / 無関係 **0.4518** / 差 **0.1388** |
| 兄弟の類似度 | 無関係を上回る | 兄弟 **0.6180** / 差 **0.1556**、兄弟120体すべて非同一 |
| 10世代交配後の多様性 | 初期の50%以上 | 初期 0.8437 → **0.6412（保持率 76.0%）** |
| 潜性形質の発現率 | 0回でないこと（理論値25%） | うすばね 26.0% / わがさ 22.5% / ほしぞら 25.3% / はなびら瞳 22.3% |
| 突然変異率 | 設定どおり | カテゴリ **1.689%**（設定1.8%）/ 素体 **0.650%**（設定0.6%） |
| 変異が系統を保つか | 隣接対立遺伝子へ寄る | 変異先の距離が平均1.41、**距離1が73.5%** |
| 500体生成 | 例外0 | 例外0、parts に 'undefined' 0件、保因保持 500/500 |
| 1000回交配 | 例外0 | 例外0 |
| 素体の分布 | 偏らない | slime 33.8% / maru 33.0% / yurei 33.2% |

**希少度の到達性**（リードが差し戻して修正させた項目）

| 集団 | rare | precious | score最大 |
|---|---|---|---|
| 初期卵 500体 | 2.8% | 0.6%（3体） | 81 |
| 交配 3系統 864体 | 12.0% | **2.9%（25体）** | 100 |

修正前は交配288体で precious が **0体**（最大score 64 / 閾値66）だった。原因は固定上限による加点の飽和。詳細は DESIGN_DECISIONS D-011。

**素体の分布（500体）**

```
maru:182  slime:147  yurei:171
```

**装飾器官の個数分布（500体・成体）** — 回帰テストとして固定

```
0個:28  1個:109  2個:161  3個:151  4個:40  5個:11  6個以上:0   平均 2.14
```

希少度の修正でこの分布が変わっていないこと（＝装飾数を増やして希少度を稼いでいないこと）を確認済み。その後の顔まわりの作り直し（DESIGN_DECISIONS D-027）でも、いじったのは口と目の**描画**と `mouth` 遺伝子座だけなので、装飾数には影響していない。

## 進行の検証（実測値）

**通しプレイ（最大効率・デバッグ操作なし）**

```
t=  0秒 操作  1回  卵を3個えらんだ
t= 68秒 操作 52回  最初の孵化 → 標本帳 解放
t=232秒 操作175回  成体化 → 展示会 解放
t=232秒 操作176回  展示会 B 59.3点 / 160コイン → ショップ 解放
t=240秒 操作186回  交配 → 卵誕生
t=312秒 操作258回  子が孵化・親子4体が共存
```

**人間らしい間欠プレイ（60〜90秒に1回、1体に1アクション）を20分**

```
レセ    健康 最低 69.4 / 機嫌 最低 45.5
ニロマ  健康 最低 74.4 / 機嫌 最低 49.0  ← 交配可能
タネラ  健康 最低 76.6 / 機嫌 最低 61.3  ← 交配可能
20分後に交配条件を満たす個体: 2体
```

**30分完全放置 → 世話再開**

```
健康 69.4→26.8 / 74.7→39.2 / 77.1→39.6（下限20を割らない）
そこから6分・世話12回で 健康 26.8 → 59.5
```

**その他**

| 項目 | 実測 |
|---|---|
| 装飾数と展示会得点の相関 | **r = 0.223**（要求 < 0.5）。派手な個体が常に勝つ設計ではない |
| 連打 vs 巡回の効率 | 巡回が **1.53倍**効率的（効果倍率 1.749 vs 2.678） |
| オフライン上限 | 24時間放置しても **2.0時間で頭打ち**。成体化 0体 |
| 展示会の決定論性 | 同条件・別時刻で total=69.9 が完全一致 |
| 世話の質の反映 | よく世話 79.1点/227コイン vs 放置 58.3点/158コイン（**20.8点差**） |
| 展示会5項目のスケール（成体100体） | 美しさ 平均67.0（30〜96）/ 健康 73.3（53〜94）/ 個性 59.7（34〜87）/ 希少性 52.4（41〜76）/ 育成 71.1（44〜96） |

## セーブの検証

| 項目 | 実測 |
|---|---|
| ラウンドトリップ | 新規state → save → load で完全一致 |
| 破損（JSON途中切断／不正な型／空文字） | 3パターンとも例外なし。バックアップから復旧 |
| 本体・バックアップ両方破損 | 起動不能にならず、内容説明のダイアログが出る |
| v1→v4 連鎖 | 全段通過し `validateState` に合格 |
| v3→v4（実際に発生したスキーマ変更） | 全個体に `lastExhibitAt`/`lastBredAt` が 0 で補完される |
| クールダウンの永続化 | JSON往復後も保持。復元直後の再参加が「あと3分」で拒否される |
| 未来バージョン（99） | null を返し起動不能にならない |
| NaN 注入 | JSON往復しても null にならない（`healBrokenStats` が復元） |

## リードによる実ブラウザ検証

実際にブラウザで新規ゲームから手で操作した結果。

| 項目 | 結果 |
|---|---|
| 通しプレイ | タイトル → 卵9個から3個選択 → 孵化 → 成体化 → 展示会（Bランク66点/170コイン）→ ショップ解放 → 購入 → アイテム使用 → 2体目成体化 → 交配解放 → 交配画面まで**デバッグ操作なしで到達** |
| **seed再現性** | 完全リロード後、SVGが**バイト単位で一致**（10,774文字／ハッシュ 656577212、インスタンス固有idを正規化後）。コイン残高・個体seedも保持 |
| **スマホ幅 393px** | 全7画面で横スクロールなし（scrollWidth = clientWidth = 394） |
| コンソールエラー | 0件 |
| 購入の視覚反映 | 陽だまりランプが育成室に表示され、ショップで「設置ずみ」表示 |
| 表情の状態反映 | 状態が悪い個体は目が半開き・口が真横になる |
| 潜性の保因表示 | 交配画面に「『たれみみ』を かくして 持つ ことも あります」と表示される |

### 成長差分（リードが同一 seed の幼体・成体を並べて目視）

`docs/screenshots/lead/growth-juvenile.png` と `growth-adult.png`（同一 seed 12 体）。

| seed | 成体で加わったもの |
|---|---|
| `N82B-9D4A` | **羽・触角・植物器官** |
| `PH54-DYQB` | **首かざり（つぶわ）** |
| `8SLK-PTB4` | **単眼・浮遊物・耳** |
| `2WSL-GUCN` | **角** |
| `DNXA-Q4AK` | **耳・尾** |
| `4GR2-FXYU` | **耳・粒子** |

**12 体中 10 体で新しい器官が発現。** 幼体は小さく丸く、目が相対的に大きく、装飾が未発達という D-005 の設計どおり。指示書 §25「幼体と成体を並べて成長が分かる」を満たす。

**残る 2 体**（`EYSR-BGQF` 緑のスライム、`HED6-BZSB` 桃のスライム）は変化が小さい。以前の批評で「25 体中 7 体が何も起きない」と指摘された分が **12 体中 2 体**まで減っている。

### 兄弟比較（リードが Visual Lab で目視）

`docs/screenshots/lab-10-siblings.png`。親A = slime/lagoon、親B = yurei/dusk から子を10体。

**注意: この画像は突然変異率 6 倍（`mutationScale=6`）での生成**で、通常プレイより変異が多い条件。その上で:

- **共通性**: 親A 由来の lagoon（青）が 4/10、親B 由来の dusk（紫）が 2/10。素体も slime 5 体・yurei 4 体で両親から受け継がれている。耳・触角も多くの子に残る
- **差異**: coral / moss / ember は 6 倍変異による新色。maru が 1 体だけ出現
- 「青紫系・丸いシルエット・小さな耳や触角」という家系の印象を保ちつつ、1 体ずつ違う

指示書 §25「複数の子に兄弟らしい共通性と違いがある」を満たす。通常の変異率ではさらに家系の印象が強くなる。

### 修正後の再確認（リード・実ブラウザ）

ゲームプレイ批評の P0 修正が、**プレイヤーから見て実際に効いているか**を確認した。スクリーンショットは `docs/screenshots/lead/`。

| 項目 | 実測 |
|---|---|
| 新規ゲーム → 卵3個選択 → 育成室 → 世話3巡 | 通常操作のみで完走。コンソールエラー **0件** |
| 「この子を 手放す」 | 個体詳細の**上部**に配置され、一目で見つかる（`play-detail-top.png`） |
| 世代表示の矛盾 | 見出し「たまご・**初代**」と遺伝情報欄「**初代**」が一致。解消を確認 |
| 個体詳細の全高 | **5100px → 1538px**。親子比較が最下部に埋もれる問題の前提が解消 |
| 卵段階の希少度 | 「めずらしさ **？**」と伏せられている（D-012 のネタバレ防止が効いている） |
| 未解放機能 | ナビに 🔒 と条件（「最初の卵が孵ると開きます。（孵化 0 / 1）」）が出る |

**この前のプレイで欠陥を1件発見し差し戻した。** 自動テストが全て合格しているのに、手で30分遊ぶと3体全員が健康0・機嫌0まで落ちた。原因は減衰:回復が6.7:1の非対称。詳細は DESIGN_DECISIONS D-016。修正後、上記の「間欠プレイ20分」テストで検証済み。

## 描画の自動検査

`inspectModel` による検査。**批評担当の指摘を受けて強化した後**の値。

検査は3段階で直した。

| 時点 | 成体300体での検出 | 内容 |
|---|---|---|
| 当初 | **0件** | 明白な破綻を1件も検出できていなかった（卵9個で「問題個体 0/9」と表示しながら、実際には装飾の破綻・宙に浮く菱形・卵を横断する巨大な草があった） |
| 強化後 | 52件相当 | 検出はするようになったが、**影の bbox を実寸の2.4倍・4.8倍で見積もっていた誤検出**が大半 |
| bbox 修正後 | **7件（2.33%）** | すべて `not-touching-body:tail` |
| 付け根判定の修正後 | **0件** | 下記のとおり誤検出だった |

最後の7件は**検査側の誤りだった**。`boxProbes` が bbox の角と辺の中点8点しか見ないため、尾の渦巻きのように**付け根が角でも辺の中点でもない**パーツで接触点を取りこぼしていた。Visual Lab の描画境界表示で該当個体（`94TD-D2EF`）を見て、尾が実際には体に接していることを確認したうえで、宣言された `anchor` を見るよう修正した。

ただし `anchor` だけを見ると「付け根の宣言はあるが絵は別の場所にある」パーツを見逃すため、**「付け根が体の上にある」かつ「パーツの絵がその付け根の周りに描かれている」の両方**を要求している。

**検査が骨抜きになっていないことは `tests/inspect.test.ts` で固定した**（6件・全合格）。

| 検出できること（合成モデルで検証） | 結果 |
|---|---|
| 体から完全に離れたパーツ | 検出 |
| 付け根の宣言と絵の位置が食い違うパーツ | 検出 |
| viewBox からはみ出したパーツ | 検出 |
| SVG に NaN が混ざっている | 検出 |

| 誤検出しないこと | 実測 |
|---|---|
| 成体300体 | 不良 < 3件 |
| 幼体200体 | 不良 < 2件 |
| 卵200体 | **0件** |

**強化前の検査は明白な破綻を1件も検出していなかった**（卵9個で「問題個体 0/9」と表示しながら、実際には装飾の破綻・浮遊・はみ出しが複数あった）。影の bbox が実寸の2.4倍・4.8倍で見積もられていた誤検出も、リードが実寸ベースに修正した。

## E2E テスト（Playwright・実行済み）

`npx playwright test` → **20 passed（PC 10 件 / スマホ Pixel 5 相当 10 件）**。

| テスト | 内容 |
|---|---|
| 中心のゲームループ | 新規ゲーム → 卵が**9個**であること → 3個選択（2個では確定できない・選び直せることも検証）→ 育成室 → 世話を巡回して**孵化**まで。孵化後に標本帳が解放されること。**コンソールエラー0件**であること |
| 解放前の機能 | 未解放画面へ直接遷移しても壊れず、条件が読めること（ダミーボタンを置かない） |
| リロード | 進行が保たれ、**SVG が完全一致**（インスタンス固有 id を正規化後） |
| 画面遷移 | 卵候補9個がリロード後も同一。育成室 → 設定 → 育成室 で見た目が変わらない |
| 横スクロール | 全7画面で `scrollWidth === clientWidth` |
| タップ対象 | 40px 未満のボタン・リンクが 0 件 |
| 壊れたセーブ | 本体・バックアップ両方を破壊しても起動不能にならず、何が起きたか説明され、操作できる導線が残る |
| **手放す操作（UI 経路のみ）** | 個体詳細から到達でき、確認ダイアログを経て実際に個体数が 3 → 2 に減る |
| **最後の 1 体で詰まない** | 残り 2 体では拒否され、理由が画面に出る。個体数は減らない |
| **世話ボタンの位置** | 育成室に着いた直後、`[data-care]` がファーストビュー内にあり、生きものと同時に見える |

後半 3 件は `e2e/03-no-dead-end.spec.ts`。**API を直接呼ばず、画面から到達できる操作だけ**で構成している。
`tests/integration.test.ts` の「所持枠が上限でも詰まない」は `releaseCreature()` を直接呼んでいたため、**どの画面からも呼ばれていない**という実際の詰みを検出できなかった（DESIGN_DECISIONS D-018）。その反省を回帰テストとして固定したもの。

**このテストで実バグを1件発見した。** ヘッダのブランドリンクが 79×27px でタップ要件を下回っていた（`min-height: var(--tap)` を追加して修正）。

E2E ヘルパには、リードが実機検証中に踏んだ落とし穴の回避策を入れてある。**アプリは離脱時に自分の状態を保存するため、`localStorage.clear()` → reload では初期化されない。** アプリを持たないページ（`sheet.html`）へ移動してから消す必要がある。

## 未実施のテスト（正直な申告）

- **展示会・ショップ・交配の E2E** — 成体化まで実時間で数分かかるため E2E には含めていない。仮想時間の `tests/progression.test.ts` が担当
- **メモリリークの検査** — 未実施
- **100体一覧の描画速度の定量測定** — シート上に表示される値（100体 lite で 78ms）以外は未計測
- **ブラウザ再起動相当・複数世代交配・所持枠上限付近での統合テスト** — 一部は Vitest で実施したが、実ブラウザでの統合ループは未実施
