# LOOP_REPORT.md — 反復記録

実装担当と評価担当を分け、評価が合格を出すまで実装・確認・批評・修正を繰り返した記録。
**実際に反復した項目のみを記載する。** 1周しかしていない項目、未着手の項目はそのように書く。

エージェント構成:

| 役割 | 所有ファイル |
|---|---|
| リードアーキテクト（統合・最終判断） | `src/core/**`, `src/genetics/loci.ts`, 各ドキュメント |
| 遺伝・シミュレーション | `src/genetics/{genotype,phenotype,breeding,similarity,naming}.ts` |
| クリーチャーアート | `src/render/**`, `sheet.html` |
| ゲームプレイ | `src/game/**`（`config.ts` を除く） |
| セーブ・アーキテクチャ | `src/save/**`, `src/game/config.ts` |
| UI・演出 | `src/ui/**`, `src/audio/**`, `src/main.ts` |
| 厳格なビジュアル批評（**書き込み権限なし**） | — |

型契約 `src/core/types.ts` はリードのみが所有し、他は読み取り専用とした。

---

## 項目: 遺伝子データ・表現型変換・交配・優性潜性・突然変異・親子の類似性

**担当**: 遺伝・シミュレーション / **評価**: リード / **反復 2 周**

### 1周目
実装後、35テスト合格。親子差 0.1388、10世代保持率 76.0%、潜性発現 22〜26%、突然変異 1.689% を実測で報告。

**リードが独立検証して不合格。** 実装担当は「precious は交配で潜性を集めれば出る」と報告していたが、リードが12世代×24体（288体）を回すと **precious が0体**（最大score 64 / 閾値66）。到達不能な tier は死んだ機能。

### 2周目
実装担当が原因を実測で特定:
1. 固定上限 `min(24, n*8)` による飽和が主因。珍しい形質の同時発現数は交配で実際に増えていた（notable 平均 0.95→1.63）のに、**3か所を超えると1点も増えなかった**
2. notable と潜性の二重計上（実質同一信号）
3. `glow > 0.72` が **500体中0体**など、閾値が厳しすぎて死んだ加点

逓減加点への変更、二重計上の解消、連続ランプ化、配色調和・珍しい組み合わせ7種・多面性ボーナスの追加で対応。

**結果**: 交配864体で precious 1.4%（12体）、rare 10.5%。**装飾数分布は修正前後で不変**（回帰テストとして固定）。

**合格。** 42テスト。

---

## 項目: セーブ・読込・データマイグレーション

**担当**: セーブ・アーキテクチャ / **評価**: リード / **反復 2 周**

### 1周目
85テスト合格。破損3パターン・v1→v3連鎖・バックアップ復旧を実測で確認。**合格。**

このとき実装担当が自らバランス上の欠陥を発見して報告した。卵の5ボタンを2秒で連打すると**世話7回・5.0秒で孵化**してしまう。`SATIATION`（直前の世話からの経過時間に比例して効果が入る）と全体クールダウン2秒を追加し、連打は6倍の操作で22%しか速くならない設計に変更。

### 2周目
ゲームプレイ担当から「展示会・交配のクールダウンがモジュール内 `Map` にあり、**リロードで消えてコイン稼ぎの抜け道になる**」と申告。

リードが型契約を変更（`Creature.lastExhibitAt` / `lastBredAt` を追加、`SAVE_VERSION` 3→4）。セーブ担当が v3→v4 マイグレーションと `coerceState` 対応を実装。

移行値を `Date.now()` ではなく **0** にしたのは、旧セーブのプレイヤーに身に覚えのない待ち時間を発生させないため。

**回帰テスト**: v3セーブを load → `lastExhibitAt` を刻む → save → 再 load で値が生き残ることを確認。従来はここで `Map` が消えていた。

**合格。** 101テスト。

---

## 項目: 卵選択・孵化・育成・成長・展示会・ショップ・交配・ゲームバランス

**担当**: ゲームプレイ / **評価**: リード（自動テスト＋実機プレイ） / **反復 3 周**

### 1周目
通しプレイが312秒・258操作でデバッグ操作なしに完走。相関 r=0.165、巡回が連打の1.53倍効率、24時間放置でも成体0体。

**リードが2点で不合格**:
- クールダウンの永続化（上記）
- **展示会の「個性」が常に16.3/100** — プレイヤーには壊れて見える

### 2周目
実装担当が実測で原因を特定: 個性の**分母を0.275に取っていた**（実測p95は0.19）。あわせて `harmony` が平均0.977でほぼ飽和、`formFit` が平均0.999で判別力ゼロという死んだ項目も発見。

`judgeScale(raw, lo, hi, gamma)` を新設し、5項目を実測分布の下端・上端で30〜96点に伸ばした。

**結果**（成体100体）: 美しさ 67.0 / 健康 73.3 / 個性 59.7 / 希少性 52.4 / 育成 71.1。全項目が40〜75に収まり、優秀な個体は85以上に届く。

### 3周目
**リードが実ブラウザで最初から手で30分プレイして不合格。**

自動テストが全て緑なのに、手で遊ぶと**3体全員が健康0・機嫌0**まで落ちた。健康+45の薬を使っても約2分で元に戻る。

原因（config から計算）: 低ステータス4種で健康 **-0.4/秒**（100→0が4分）、回復は **+0.06/秒**かつ全ステータス60以上が条件。**減衰:回復 ≒ 6.7:1 の非対称ラチェット**。

**なぜ自動テストが見逃したか**が本質だった。実装担当のテストは「全個体に4秒ごとに世話」する最大効率プレイで、**`SATIATION` 機構はその速度の世話を非効率として罰する設計**。テストが通る操作と、ゲーム自身が推奨する操作が食い違っていた。

さらに実装担当は自分のテスト側の欠陥も発見した。`applyTick` を4〜5秒刻みで呼んでいたため `OFFLINE_THRESHOLD_MS`（2秒）を超え、**全部オフライン扱い（減衰0.5倍）で測っていた**。

対応: 減衰率を約1/4に。健康モデルを**構造ごと作り直し**、「世話の質の遅れた鏡」（目安値へ指数的に接近、下限20）にして非対称を解消。「人間らしい間欠プレイ」テストを追加。

**結果**: 20分の間欠プレイ（71秒に1回）で健康最低69.4/74.4/76.6、交配可能な個体が2体。30分放置でも健康は下限20を割らず、6分・世話12回で26.8→59.5に回復。

**合格。** 167テスト。

---

## 項目: ゲノモンの見た目（素体・顔・目・口・装飾・模様・質感・卵・成長差分）

**担当**: クリーチャーアート / **評価**: 厳格なビジュアル批評（別エージェント）＋リード / **反復 3 周・現在も不合格**

### 1周目
SVG で3素体・目7種・口6種・装飾器官10種・模様8種・質感7種・卵を実装。3000モデル検査で問題0件と報告。

**リードが5点で不合格**: 顔が全個体同じ／配色が緑青ばかり／縞がバーコード／卵が小さく脈が落書き／足が針金。

### 2周目
実装担当が対応。リードの再確認では配色・縞・足が明確に改善、顔は部分的改善に留まる。

### 3周目
**厳格なビジュアル批評担当（実装担当とは別）が不合格。平均 5.13/10、16項目中15項目が7点未満。**

批評が発見した、実装担当もリードも見落としていた問題:
- **自動検査が機能していない** — 卵シートが「問題個体 0/9」と表示しながら、実際には装飾の破綻・浮遊・はみ出しが複数あった
- 脚の体内部分が透けて台形として見える（本体塗りの透明度で下層が透過）
- まぶたの線が輪郭外へ突き抜ける
- 卵に成体の耳/角がそのまま適用され幾何ブロックとして破綻、菱形が宙に浮く
- 卵の放射状アーティファクト
- slime のシルエット分散がほぼゼロ（16体中14体が同一ドーム）

**リードが実物で裏を取り、指摘は正当と判断。** とくに卵についてはリード自身の当初評価（ゲーム内の小サイズで見て「形・色・模様がはっきり違う」）が甘かった。

対応した分:
- 検査を強化（100体で3件を検出するようになった）
- 影の bbox が実寸の2.4倍・4.8倍で誤検出していた分をリードが実寸ベースに修正（25体中13件の誤検出が消えた）
- 目の白目形状を3種に分離、**点目を追加して結線**（リードが完了させた）
- 遺伝子カタログ側でリードが対応: 『まるめ』の顕性度・重みを下げ、『むじ』を減らし、配色を寒色67%→44%に是正

**現在も不合格。** 未修正の指摘は VISUAL_QA.md に列挙。担当エージェントがセッション上限で中断したため、P0の描画バグ8件の大半が残っている。

---

## 項目: UI・画面遷移・レスポンシブ・チュートリアル・音・演出

**担当**: UI・演出 / **評価**: リード（実機） / **反復 1 周**

9画面・24ファイル・約5,370行を実装。Web Audio API による合成音22種＋seedと性格で変わる鳴き声。

実装担当が自ら見つけて直した実バグ: ゲージが全画面で不可視（`<i>` がインラインで width/height が効かない）／`.btn--sm` が36pxでタップ要件違反／タイトル画面でナビが消えない／育成室の空状態が無い。

**リードが実機で確認**: 全7画面でスマホ幅393pxの横スクロールなし（scrollWidth = clientWidth = 394）、コンソールエラー0件、seed再現性がリロード後もバイト単位で一致。

**1周のみ。** 厳格なUI批評は未実施。

---

## 項目: ゲームプレイ全体（分かりやすさ・作業感・愛着・詰み）

**担当**: UI・演出 / **評価**: 厳格なゲームプレイ批評（別エージェント・書き込み権限なし） / **反復 1 周**

### 1周目 — **不合格**（10観点中 4 項目が 7 点未満）

批評担当が新規セーブから通常プレイで通し、PC 1280×860 とスマホ 393×851 の両方を実測した。

| 観点 | 点 |
|---|---|
| 作業感が強すぎないか | **2** |
| 進行不能・詰みがないか | **2** |
| 退屈ではないか | 4 |
| 何をすればよいか分かるか | 5 |
| 待ち時間 | 5 |
| 報酬の分かりやすさ | 5 |
| 愛着 | 5 |
| 系統づくり | 5 |
| 交配への動機 | 6 |
| スマホ操作性 | 6 |

**通しプレイの実測**: タイトル → 親子比較まで **約9分10秒 / 179 クリック**（1体目のみ。残り2体を育てるにはさらに約13分・約300クリック）。

**決定的な2件**（リードがコードで裏付け）

1. **詰みが実在した。** 成体3/3・幼体3/3 で幼体が growth 100 に達すると「どれかを手放すか」と案内されるが、**`releaseCreature` はどの画面からも呼ばれていなかった**（呼び出しは `src/dev/devMode.ts` のみ＝製品ビルドに含まれない）。脱出手段はセーブの全消去のみ。→ DESIGN_DECISIONS **D-018**
2. **UI が「連打しろ」と誘導していたが、連打は損だった。** 連打 299秒/114クリック vs がまん 191秒/8クリック。**106クリック多く押した方が108秒遅い。**→ DESIGN_DECISIONS **D-019**

その他: 育成室に着いた瞬間 世話ボタンが画面外（PC top=844/860、スマホは下部ナビの裏で不可視）／孵化直後に未使用の6ボタン全部が「あきているみたい」／展示会が審査前にヘッダで結果をネタバレ／交配の80コインが無告知／世代表示の矛盾（「2代目」と「初代」が同じカードに同居）。

### 修正（P0 4件・P1 8件・P2 4件すべて対応）

| 項目 | 修正後の実測 |
|---|---|
| 手放す UI | 個体詳細に実装＋確認ダイアログ。目標行から**枠を埋めている個体**へ直接飛ぶ。詰み状態を作って `3体 → 手放し → 2体` を通し、残り2体でのガードも動作 |
| 連打誘導 | 文言を「連打しても 早くは なりません」に変更。**「あと ○ 秒で 全効果」のカウントダウンを新設**（12%/あと17秒 → 40%/あと12秒 → 100%/いまが 押しどき！） |
| 「あきている」の誤表示 | `av.reason` をそのまま表示。孵化直後の注記が **6件 → 1件** に |
| 世話ボタンの位置 | PC **844 → 282**（画面高860）、スマホ **809 → 425**（ナビ top 663）。生きもの（192–370）と世話ボタンが**重ならず同時に見える** |
| 展示会のネタバレ | 審査中はコイン 361 のまま、獲得カットで 586 に更新 |
| 親子比較の位置 | **5100px の最下部 → 全高2188px の 637px 地点**（PC ファーストビュー内） |
| 世代表示 | `generation` は game 側で 1 始まりだった（`+1` が誤り）。`generationLabel()` に集約して統一 |

**回帰ガード**: リードが `e2e/03-no-dead-end.spec.ts` を追加し、「手放す操作が**画面から**到達できること」「残り2体では拒否され理由が出ること」「世話ボタンがファーストビューにあること」を UI 経路のみで固定した。API を直接叩く検証では詰みを検出できないため（D-018）。

**再評価は未実施。**

---

## 未着手の項目

以下は指示書に含まれるが**1周も回していない**。

- **Visual Lab**（§19）— `src/dev/` は空。担当エージェントが2度ともセッション上限で中断
- **開発者モード**（§20）— 同上
- **E2E テスト**（§28）— `playwright.config.ts` のみで `e2e/` は空
- **厳格なゲームプレイ批評**（§21）— 未実施。リードの実機プレイで代替したが、専任の批評担当は立てていない
- **統合ループ**（§23）— 未実施
- **アクセシビリティの専任評価** — UI担当の自己確認のみ
- **パフォーマンスの定量評価** — シート上の表示値以外は未計測

## 反復回数のまとめ

| 項目 | 反復 | 状態 |
|---|---|---|
| 遺伝（遺伝子・表現型・交配・突然変異・類似性） | 2 | 合格 |
| セーブ・マイグレーション | 2 | 合格 |
| ゲーム進行・バランス | 3 | 合格 |
| ゲノモンの見た目 | 3 | **不合格（継続中）** |
| UI・演出・音 | 1 | 未評価（批評未実施） |
| Visual Lab / 開発者モード / E2E / 統合ループ | 0 | 未着手 |
